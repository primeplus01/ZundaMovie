- ANM1(アニメーション効果) -

	○明滅登場
		透明度を明滅させて登場又は退場します。
		◇登場、退場
			明滅する時間をフレーム数で指定します。

	○斜めブラインド
		斜め方向にブラインドして登場します。
		◇割合
			ブラインドする割合(%)を指定します。
		◇幅
			一つのブラインドの幅を指定します。
		◇角度
			ブラインドの角度を指定します。
		◇時間
			「時間指定で登場」にチェックした場合に有効な項目。
			指定した時間で登場又は退場します。
			マイナスの値で退場します。
		◇時間指定で登場
			ブラインドを時間指定で登場又は退場するようにします。

	○マスクPlus
		画像にマスクの形で穴を空けます。
		重ねがけすることで複数の穴を空けることができます。
		◇X,Y
			マスクの中心座標を指定します。
		◇サイズ
			マスクのサイズを指定します。
		◇回転
			マスクの回転角度を指定します。
		◇マスクの種類
			マスクの種類を指定します。
		◇ライン幅
			マスクのライン幅を指定します。
		◇Xスケール、Yスケール
			マスクの縦横比を、XY軸ごとのサイズ(%)で指定します。
		◇透明度
			マスク部分の透明度を指定します。
		◇ぼかし
			マスクの境界をぼかします。

	○片側ぼかし
		上下左右の境界から透明度をぼかします。
		◇上、下、左、右
			各方向からの範囲(ピクセル)を指定します。

	○ランダムラスター
		大小2つのラスターをランダムな間隔でかけます。
		ノイズ表現に良いかもしれません。
		◇横幅(小)
			小さなラスターの横幅の大きさを指定します。
		◇横幅(大)
			大きなラスターの横幅の大きさを指定します。
		◇閾値(小)
			小さなラスターがかかる頻度を設定します。
			まず、大きなラスターの判定がなされ、次に小さなラスターの判定がされます。
			なので基本的に「閾値(小)＜閾値(大)」という関係になるように設定して下さい。
		◇閾値(大)
			大きなラスターがかかる頻度を設定します。
		◇横方向のみ、縦方向のみ
			どちらか片方にチェックした場合は、横または縦方向のみにラスターがかかります。
			両方ともチェックなし又は両方ともチェックがある場合は、縦と横方向にランダムにラスターがかかります。
		◇高さ
			ラスターの高さを指定します。
			数値を大きくすると波のようなイメージになります。
		◇色ずれ幅[%]
			ラスターの程度に応じて色ずれがかかるようになります。
			数値は、ラスターの横幅×？％の値となります。
		◇色ずれ種類
			色ずれの種類を指定します。
			0=赤緑、1=赤青、2=緑青

	○自動方向ブラー
		XY軸方向への移動・速さに応じて自動的に方向ブラーがかかります。
		トラックバーで移動した場合のみ有効です(スクリプトでの移動には非対応)。
		◇ブラー
			ブラーの強さ(範囲)を指定します。
		◇色ずれ[%]
			色ずれを付加することができます。
			数値は、ブラーの範囲×？％の値となります。
		◇ずれ種類
			色ずれの種類を指定します。
			0=赤緑、1=赤青、2=緑青

	○振動Plus
		複数の振動フィルタを重ねることで複雑な振動をさせます。
		◇振幅
			基準となる振幅の大きさを指定します。
			大きくするとオブジェクトがどこかへ消えやすくなってしまいます。
		◇周期
			基準となる振動の周期を指定します。
		◇個数
			重ねがけする振動の数を指定します。
			数を多くすると振動が相殺されて緩やかになります。
		◇閾値
			ゼロのときは常に振動します。通常はゼロのままが推奨。
			ゼロ以外の値に設定した場合は、ランダムに静止状態になります。
		◇z軸振動の有無
			Z軸方向にも振動するかどうかを選択します。

	○twitchっぽいもの
		振動、放射ブラー、発光、色ずれの4つのフィルタをセットにして適用します。
		ノイズ表現のひとつに。
		◇閾値
			フィルタがかかる頻度を指定します。
		◇強さ
			放射ブラー、発光、色ずれの強さを指定します。
		◇発光閾値
			発光フィルタのしきい値を指定します。
			100に近づけるほど、発光しにくくなります。
		◇振動周期
			振動フィルタの周期を指定します。
		◇X,Y,Z振動
			X,Y,Z軸方向のそれぞれの振動の強さを指定します。
			ゼロにすると、その軸方向には振動しなくなります。
		◇揺れ幅倍率
			振動フィルタは大きい振動と、小さな振動の二つが重ねがけされています。
			「振動(大)＝振動(小)×揺れ幅倍率」という関係になっており、
			例えば、揺れ幅倍率をゼロにすると、大きな振動は無くなり、小さく振動するのみとなります。
		◇色ずれ幅[%]
			色ずれ幅の大きさを％で指定します。
		◇色ずれ種類
			色ずれの種類を指定します。
			0=赤緑、1=赤青、2=緑青

	○塗りつぶし
		オブジェクトを任意の色で塗りつぶしたりします。
		簡単に画像をシルエット化します。
		◇モノクロ
			単色化フィルタをかけ、モノクロにします。
			おまけの項目ですが、モノクロにしてから合成すると雰囲気が変わる場合もあります。
		◇強さ
			塗りつぶしの強さ(透明度)を指定します。
		◇合成
			合成モードと同様に、塗りつぶしの合成方法を指定します。
			0=通常、1=加算、2=減算、3=乗算、4=スクリーン、5=オーバーレイ、6=比較(明)、7=比較(暗)、8=輝度、9=色差
		◇色
			塗りつぶしの色を指定します。

	○段階グラデーション
		任意の2色を指定し、段階的なグラデーションをかけます。
		◇幅
			グラデーション部分の幅を指定します。
		◇分割数
			グラデーション部分を何段階にするか、分割数を指定します。
		◇角度
			グラデーションの角度を指定します。
		◇色1、色2
			グラデーションにする任意の色2色を指定します。
		◇中心X、中心Y
			グラデーションの中心座標を指定します。

	○円形並べ
		同じ画像をXY平面上に円形に並べます。
		◇個数
			画像の数を指定します。
		◇距離
			円の半径を指定します。
		◇拡大率
			画像の拡大率を指定します。
		◇回転
			画像の回転角度を指定します。
		◇回転なし
			チェックすると画像は、円の配置し応じて角度が変化しなくなり、同じ角度になります。
		◇個別基準
			チェックすると、設定ダイアログの拡大や回転が、個別の画像基準になります。
			チェックなし：設定ダイアログの拡大回転→全体の拡大回転
			　　　　　　　ANM効果の拡大回転→個別画像の拡大回転
			　　　　　　　上のオブジェクトでクリッピングにも対応。
			　　　　　　　カメラ制御などで使用しやすい。
			チェックあり：設定ダイアログの拡大回転→個別画像の拡大回転
			　　　　　　　ANM効果の拡大回転→全体の拡大回転
			　　　　　　　上のオブジェクトでクリッピングには対応せず。

	○円形並べ-カメラ制御
		同じ画像をXZ平面上で立体的に円形に並べます。カメラ制御下で使用して下さい。
		◇個数
			画像の数を指定します。
		◇距離
			円の半径を指定します。
		◇拡大率
			全体の拡大率を指定します。
		◇回転
			全体を回転させます。
		◇Y軸回転なし
			配置に応じて、円形にY軸回転していますが、
			チェックをすると、Y軸回転せず、同じ方向を向くようになります。

	○円形に並べる(順)
		同じ画像をXY平面上に円形に並べます。
		順番に登場または退場するように設定することが出来ます。
		◇個数
			画像の数を指定します。
		◇距離
			円の半径を指定します。
		◇時間
			ひとつの画像が登場する時間(秒)を指定します。
		◇間隔
			次の画像が登場してくるまでの間隔(秒)を指定します。
		◇外向き
			チェックすると、円形の並びが外向きになります。

	○円移動
		画像が円の軌道を描いて回り続けます。
		◇半径
			円軌道の半径を指定します。
		◇速さ
			移動の速さを指定します。速さ=1000だと1秒間に1周します。
		◇内向き
			チェックすると、画像の向きを円の内向きにします。

	○ランダム配置
		同じ画像をランダムな座標・角度で配置します。
		◇個数
			配置する画像の数を指定します。
		◇範囲
			散らばる範囲を一括して調整します。
			ゼロにすると中心に収束し、トラックバーを直線移動することにより、
			だんだんと散らばっていくように表現することも出来ます。
		◇移動速度
			移動速度を設定すると、各画像がランダムな方向・速度で移動します。
		◇回転速度
			回転速度を設定すると、各画像がランダムな方向・速度で回転します。
		◇X,Y,Z軸範囲[%]
			X,Y,Z軸ごとに散らばる範囲の基準を指定します。
			数値は％で画面サイズが基準となっています(100%=画面サイズ)。
		◇X,Y,Z回転
			X,Y,Z軸ごとに、ランダムに回転させるか指定します。
		◇拡大最小[-%]
			画像の拡大率をランダムにします。
			数値は、最小の拡大率をマイナスで指定し、その範囲でランダムな拡大率になります。
			例えば、拡大最小=80(-%)と設定すると100-80=20%→20%～100%の間でランダムな拡大率となります。
		◇乱数
			各種のランダムに設定された結果を、別のランダム結果に変更します。
			数値は適当な整数を入力すればよいので、好みのランダム結果が表示されるようにいろいろ試すとよいかもしれません。
		◇ランダム固定
			チェックすると、オブジェクトのレイヤーを移動しても、ランダム結果が固定となります。	

	○画像ループ2
		同じ画像を複数並べて表示します。
		本家の「画像ループ」フィルタと異なり、画像間に隙間を空けること等ができます。
		ただし、本家のように一定の速度でループ移動させることはできません。
		◇X,Y間隔
			各画像間の間隔を、XY方向にそれぞれ指定します。
		◇X,Y個数
			各軸方向にループする回数を指定します。
		◇Z間隔
			Z軸方向にループした場合に、Z軸方向の間隔を指定します。
		◇Z個数
			Z軸方向にループする回数を指定します。
		◇XY軸段違い
			XY軸平面上で段違いにループします。
		◇ZY軸段違い
			ZY軸平面上で段違いにループします。
		◇個別基準
			チェックすると、設定ダイアログの拡大や回転などが、個別の画像基準になります。
			チェックなし：設定ダイアログの拡大回転→全体の拡大回転
			　　　　　　　X,Y間隔の項目が画像基準となります(単位:％)。
			チェックあり：設定ダイアログの拡大回転→個別画像の拡大回転
			　　　　　　　X,Y間隔の項目が距離基準となります(単位:ピクセル)。

	○画像ループ3
		画像をスクロール移動させます。
		本家の「画像ループ」フィルタと異なり、トラックバーでスクロールを調整することが出来ます。
		ただし、本家のように画像を複数描画することはできません。
		◇X,Y軸
			X,Y軸方向に、それぞれトラックバーでスクロールさせます。
			直線移動させることで、スクロールをだんだん速くしたり、遅くすることが出来ます。
		◇初速X,Y
			一定の速度で、X,Y軸方向にスクロールします。
			数値は1秒間に進む距離です。
		◇加速度X,Y
			加速度を設定することにより、加速又は減速することが出来ます。

	○一方向に並べる
		同じ画像を一方向に増やしていきます。
		画像ループの一種のようなものです。図形などに適用するとよいかもしれません。
		◇個数
			画像の個数です。
			トラックバーで直線移動させることで、オブジェクトが一方向に増えていくようにすることができます。
		◇間隔
			画像間の間隔を指定します。数値は距離(ピクセル)です。
		◇角度
			伸びていく方向を指定します。
		◇回転軸
			度の平面軸に沿って回転し、伸びていくか指定します。
			0=XY平面、1=XZ平面、2=YZ平面
		◇双方向
			オブジェクトが双方向に増えていきます。

	○パーティクル再現
		カメラ制御などで使用できるようにパーティクル出力を再現したものです。
		本家の「パーティクル出力」とは使い方や性質が異なるので注意。
		◇頻度
			パーティクルの出力頻度を指定します。
			数値は1秒間に出力されるパーティクルの数を表します。
		◇距離
			パーティクルが出力される距離を指定します。
			本家パーティクルとは異なり、この距離の間だけパーティクルが表示されます。
			長くしすぎると処理が重くなるので注意。
		◇速度
			パーティクルの出力される速度を指定します。
		◇方向
			パーティクルの出力される方向を指定します。下方向がゼロ度です。
		◇ランダム方向
			指定した角度の範囲でランダム方向に出力されます。360度で全方向です。
		◇透過速度[%]
			透明になる速度を指定します。
			透過速度=100で、ちょうどパーティクルの終点で透明度がゼロになるようにフェードします。
		◇拡大速度[%]
			オブジェクトが段々と拡大・縮小する速度です。
		◇ランダム速度[-%]
			パーティクルの出力される速度をランダムにします。
			数値は、例えば、ランダム速度=80で設定すると、100-80=20%→20%～100%の範囲でランダムとなります。
		◇ランダム拡大[-%]
			パーティクル一つひとつの拡大率をランダムにします。
			数値は、例えば、ランダム拡大=80で設定すると、100-80=20%→20%～100%の範囲でランダムとなります。
		◇ランダムZ角度
			出力時の角度がランダムになります。360度で完全にランダムな角度となります。
		◇X,Y,Z軸ランダム
			各座標から一定の範囲からランダムに出力されるようになります。
			例えば、X軸ランダム=800と設定すると、現在のX座標の位置から-400～400の範囲からランダムに出力されます。
		◇回転速度
			回転する速度を指定します。
			数値は、ひとつのパーティクルが発生してから消えるまで回転する角度を表します。
			例えば、回転速度=360とすると、発生してから消えるまでに1回転します。
		◇XY回転あり
			チェックするとX軸方向、Y軸方向にも回転し、3D的に回転するようになります。
		◇Z軸ランダム方向
			Z軸方向にも、ランダムな方向・距離で移動するようになります。
			数値は、ひとつのパーティクルが発生してから消えるまでに移動する距離の最大値です。

	○キラキラ
		同じ複数の画像が、1点から全方向に同時に出力されます。
		パーティクル出力の別バリエーションのようなもの。
		図形の星型に適用すると、星が散らばっていくようなイメージになります。
		◇個数
			出力される画像の個数を指定します。
		◇距離
			パーティクルが進む距離を指定します。
		◇速度
			パーティクルが進むされる速さを指定します。
		◇出現範囲
			パーティクルの初期位置を指定します。
			ゼロのときは1点から出力され、値を設定すると、その範囲内のランダム位置が初期位置となります。
		◇ランダム速度[-%]
			パーティクルの進む速さをランダムにします。
			このエフェクトでは、全てのパーティクルが同時に消えるので、出力される距離がバラバラになります。
			数値は、例えば、ランダム速度=80で設定すると、100-80=20%→20%～100%の範囲でランダムとなります。
		◇透過速度[%]
			透明になる速度を指定します。
			透過速度=100で、ちょうどパーティクルの終点で透明度がゼロになるようにフェードします。			
		◇拡大速度[%]
			オブジェクトが段々と拡大・縮小する速度です。
		◇ランダム回転
			出力時の角度がランダムになります。360度で完全にランダムな角度となります。
		◇回転速度
			回転する速度を指定します。
			数値は、ひとつのパーティクルが発生してから消えるまで回転する角度を表します。
			例えば、回転速度=360とすると、発生してから消えるまでに1回転します。
		◇ランダム拡大[-%]
			パーティクル一つひとつの拡大率をランダムにします。
			数値は、例えば、ランダム拡大=80で設定すると、100-80=20%→20%～100%の範囲でランダムとなります。
		◇減速度[1-5]
			パーティクルの移動が、だんだんと減速するように設定します。
			減速度=1のときは減速移動なしで、数値が大きいほど減速度合いが強くなります。
		◇終点で消える
			チェックすると、パーティクルの終点でオブジェクトが消えます。
			チェックを外すと、終点においてもパーティクルが残り続けます。
		◇乱数
			各種のランダムに設定された結果を、別のランダム結果に変更します。
			数値は適当な整数を入力すればよいので、好みのランダム結果が表示されるようにいろいろ試すとよいかもしれません。

	○各辺を伸縮させる
		画像の各辺を伸縮させます。
		◇上辺、右辺、下辺、左辺
			画像の各辺を伸縮させます。数値はパーセント。
		◇上辺中心、右辺中心、下辺中心、左辺中心
			伸縮させる時の中心位置を指定します。
			数値は、ゼロのときは中心、+100のときは右端(下端)、-100のときは左端(上端)となります。

	○しきい値フィルタ
		任意のフィルタ効果を、ランダムな間隔で適用させます。
		◇しきい値
			フィルタ効果がかかる頻度を指定します。
		◇間隔
			フィルタ効果がかかる時間間隔を指定します。
			例えば、間隔=0.1の場合は0.1秒ごとに、しきい値に応じてフィルタ効果をかけるかどうか判定されます。
			小さな値だと、細かくフィルタ効果がかかったり、かからなかったりするイメージです。
		◇フィルタ名
			適用したいフィルタ名を入力します。
			デフォルトでは「ぼかし」フィルタを例示として入力しています。
			※フィルタ名や項目名は正確に入力する必要があります。
		◇項目
			フィルタの項目名を入力します。最大5つの項目名を入力することが出来ます。
		◇値
			項目に対応した数値を入力します。

	○ランダム点滅
		画像等をランダムな間隔で点滅させます。
		◇しきい値
			透明になる頻度を指定します。
		◇透明度
			透明になった場合の透明度を指定します。
		◇間隔
			透明になる時間間隔を指定します。
			例えば、間隔=0.1の場合は0.1秒ごとに、しきい値に応じて点滅するかどうか判定されます。
			小さな値だと、細かく点滅するイメージです。

	○伸縮登場
		画像等が伸縮しながら登場してきます。
		画像の中心点を下の方してもいいかもしれません。
		◇時間
			登場する時間を指定します。
		◇変化増[%]
			伸縮の大きさを調整します。
		◇X軸1,X軸2,Y軸1,Y軸2
			伸縮する際の、横方向および縦方向の伸縮率(スケール)を数値でしていしています。
			この数値を変更することで、個別に値を設定することができます。
			例えば、X軸の場合「拡大率0%→X軸1%→X軸2%→拡大率100%」のように設定されています。

	○ぽよよーんと登場
		画像等がぽよよーんと伸縮して登場してきます。
		◇時間
			登場する時間を指定します。マイナスの値で退場します。
		◇勢い
			登場する勢いを指定します。
			値が大きいほど勢いよく登場し、複数回伸縮を繰り返します。
		◇揺れ幅[%]
			登場した時の伸縮する揺れ幅を指定します。
		◇種類[0-4]
			登場するパターンをしていします。
			0=拡大縮小して登場
			1=縦方向の伸縮度が変化しながら登場
			2=横方向の伸縮度が変化しながら登場
			3=縦方向の伸縮度が伸びながら登場し、縦横方向に揺れながら登場
			4=横方向の伸縮度が伸びながら登場し、縦横方向に揺れながら登場

	○モザイクマスク
		モザイク状に画像をマスクします。
		◇サイズ
			モザイクのサイズを指定します。
		◇閾値
			マスク部分の割合を指定します。
			トラックバーで移動させることにより、モザイク状のワイプの代わりとしても使用できます。
		◇乱数
			各種のランダムに設定された結果を、別のランダム結果に変更します。
			数値は適当な整数を入力すればよいので、好みのランダム結果が表示されるようにいろいろ試すとよいかもしれません。

	○格子状マスク
		格子状に画像をマスクします。
		◇サイズ
			格子のサイズを指定します。
		◇隙間
			格子の隙間部分の幅サイズを指定します。
			トラックバーで移動させることにより、ワイプの代わりとしても使用できます。
		◇反転
			マスク部分を反転させます。

	○ギザ円クリッピング
		ギザギザの円状に画像をマスクします。
		◇サイズ
			一つの四角形のサイズを指定します。
		◇半径
			円の半径を指定します。
		◇中心X,Y
			円の中心座標を指定します。
		◇反転
			マスク部分を反転させます。

	○分割登場-スライス
		画像を横または縦にスライスして登場させます。
		◇時間
			登場する時間(秒)を指定します。マイナスの値で退場します。
		◇間隔
			次のスライスが登場する間隔(秒)を指定します。
		◇分割数
			スライスする分割数を指定します。
		◇モード
			「モード」を変更することで、登場してくる方向や順番を変えることが出来ます。
		◇縦方向
			チェックすると縦方向(Y軸方向)からの登場に変えることが出来ます。
		◇縦分割
			チェックすると縦方向にスライスします。
		◇交互
			チェックすると左右(上下)に交互に登場してくるようになります。
		◇加減速[1-5]
			減速して滑らかに登場(加速して滑らかに退場)させるようにします。
			減速度=1のときは減速移動なしで、数値が大きいほど減速度合いが強くなります。
		◇登場距離[%]
			登場してくる距離を調整します。
			100%のときは画面サイズの距離から登場してきます。

	○分割登場-回転
		画像をスライスし、一片が回転しながら登場(退場)します。
		◇時間
			登場する時間(秒)を指定します。マイナスの値で退場します。
		◇間隔
			次のスライスが登場する間隔(秒)を指定します。
		◇回転
			回転する角度を指定します。
		◇分割数
			スライスする分割数を指定します。
		◇フェード
			チェックすると、一片毎にフェードして登場してきます。
		◇縦分割
			チェックすると縦分割になり、X軸方向に回転して登場するようになります。
		◇逆順
			チェックすると登場する順番が逆になります。
		◇加減速[1-5]
			減速して滑らかに登場(加速して滑らかに退場)させるようにします。
			減速度=1のときは減速移動なしで、数値が大きいほど減速度合いが強くなります。

	○分割登場-四角
		画像が四角に分割されて登場(退場)します。
		◇分割数
			画像を分割する数を指定します。
			値を大きくすると極端に重くなるため注意。
		◇時間
			登場する時間(秒)を指定します。マイナスの値で退場します。
		◇方向
			登場してくる方向を指定します。上方向がゼロ度。
		◇散らばり
			登場してくる方向の散らばり具合を、角度で指定します。
			360度で、全方向から登場してきます。
		◇フェード
			チェックするとフェードして登場します。
		◇登場距離[%]
			登場してくる距離を％で指定します。
			画面サイズが基準となっています。
		◇渦角度
			登場してくる際の回転角度を指定します。
			360度だと、1回転して渦を巻くようにして登場してきます。
		◇Z距離
			Z軸方向から登場してくるように指定します。
		◇縦横比
			縦横比を変化した状態から登場してくるように指定します。誰得項目。
		◇加減速[1-5]
			減速して滑らかに登場(加速して滑らかに退場)させるようにします。
			減速度=1のときは減速移動なしで、数値が大きいほど減速度合いが強くなります。

	○ロール-カメラ
		画像等を丸めるように曲げることが出来ます。
		カメラ制御下で使用して下さい。
		◇屈曲率
			曲げる割合を指定します。
		◇中心[%]
			屈曲する際の中心を指定します。
			数値は、-100で左端(上端)、+100で右端(下端)となります。
		◇縦ロール
			チェックすると縦方向のロールとなります。
		◇反転
			チェックすると屈曲する向きを反転させます。
		◇軸を中心に
			チェックするとオブジェクトの中心が、円筒の軸になります。
			オブジェクトをXYZ回転させる場合に役に立つ場合があります。
		◇分割数
			数値を大きくすると、より滑らかな曲面になります。
			ただしその分重くなるので注意。通常は変更する必要はない項目かもしれません。

	○球体分解-カメラ
		画像を凸(凹)面上に変形させます。
		カメラ制御下で使用して下さい。
		◇屈曲率
			曲げる割合を指定します。
		◇中心X、中心Y
			屈曲する際の中心を指定します。
		◇反転
			チェックすると屈曲する向きを反転させます。

	○歪曲-カメラ
		画像を平面上で歪曲させます。
		カメラ制御下で使用すること推奨です。
		※カメラ制御で使用すると、細い黒線などが表示されずキレイに描画されます。
		◇歪曲
			歪曲する割合を指定します。
		◇中心X、中心Y
			歪曲する際の中心を指定します。
			数値は、-100で左端(上端)、+100で右端(下端)となります。
		◇固定位置[0-2]
			歪曲した際の固定する位置を指定します。
			0=画像の対角線の大きさを固定したまま歪曲します。
			1=画像の横幅を固定したまま歪曲します。
			2=画像の縦幅を固定したまま歪曲します。
		◇分割数
			数値を大きくすると、より細かく描画されます。
			ただしその分重くなるので注意。通常は変更する必要はない項目かもしれません。

	○XYスケール
		X軸方向、Y軸方向それぞれ別個にサイズを変更し、縦横比を変えることが出来ます。
		こちらの動画で配布されているものですが、非常に便利なのでここにも記載しています。
		http://www.nicovideo.jp/watch/sm13546528
		◇Xスケール、Yスケール
			各軸方向のサイズを指定します。

	○砕け散る2
		本家「砕け散る」を改変したものです。
		本家フィルタは砕け散る時間を設定しますが、
		こちらはトラックバーの手動で砕け散る程度を設定します。
		◇経過時間
			時間経過を指定します。
			トラックバーを直線移動させることで、砕け散るアニメーションをすることができます。
			手動で動かすので、途中で停止したり、逆再生することも調整しやすくなっています。

	○砕け散る3
		「砕け散る2」を仮想バッファにも対応させたものです。
		縁取り等のフィルタ効果や「上のオブジェクトでクリッピング」が適用可能です。
		※3D的に砕け散るように見えますが、平面上に描画されています。
		◇経過時間
			時間経過を指定します。
		◇仮想bufサイズ[%]
			仮想バッファのサイズの範囲内で、画像が描画されます。
			100%で画面サイズとなります。

	○RGB色調補正(加減算)
		R,G,Bの各色を加減算合成します。
		◇R,G,B
			各色を合成します。
			プラスの場合は加算合成、マイナスの場合は減算合成します。

	○RGB色調補正(オーバーレイ)
		R,G,Bの各色をオーバーレイで合成します。
		コントラストを調整するイメージに近いかもしれません。
		◇基準
			合成する基準を指定します。
		◇R,G,B
			各色をオーバーレイで合成します。


- ANM2(アニメーション効果) -

	○n分の1拡大
		Z軸座標を直線移動させたのと同じ割合で、画像の拡大率を変化させます。
		◇100/n
			画像の拡大率を100/n倍します。
			直線移動させると、Z軸座標を直線移動させたのと同じ割合で拡大率が変化します。

	○2次ベジェ曲線
		画像等をベジェ曲線に沿って移動させます。
		※このアニメーション効果を適用する際は、設定ダイアログに追加するのではなく、
		　メディアオブジェクトの追加→フィルタ効果の追加→アニメーション効果、を選択し、
		　レイヤー上にフィルタ(水色のオブジェクト)を配置して使用して下さい。
		　画像をの座標を移動させると、任意の区間(フィルタの範囲)の移動をベジェ曲線に沿った移動にします。
		◇制御X,制御Y,制御Z
			制御点の座標を指定します。
			制御点の位置によって、ベジェ曲線の形が変わります。
		◇補助線表示
			チェックすると補助線が表示されます。
		◇補助線色
			補助線の色を変更することが出来ます。

	○3次ベジェ曲線
		画像等をベジェ曲線に沿って移動させます。
		使用方法は「2次ベジェ曲線」と同様ですが、
		こちらは制御点を2つ設定することができ、より複雑な曲線を描くことが出来ます。
		◇始制X,始制Y,終制X,終制Y
			始点の制御点と終点の制御点の座標を指定します。
		◇始制Z,終制Z
			制御点のZ軸座標を指定します。
		◇補助線表示
			チェックすると補助線が表示されます。
		◇制御点色
			補助線の色を変更することが出来ます。

	○グループ制御もどき(親)
		グループ制御の中で、さらに孫グループが作れるように作成したものです。
		まず、図形などの適当なオブジェクトに(親)を適用して下さい。
		※(親)は「グループ制御」オブジェクト単体なので、これ単体では何も表示されません。
		次に、孫グループにしたい画像などを、下の隣接するレイヤーに配置し、(子)を適用します。
		(親)のパラメータを動かすとグループ全体を移動・回転することができます。
		◇透明度2
			孫グループ全体の透明度を変化させる場合は、こちらの「透明度2」で変化させて下さい。
		◇角度
			孫グループ全体を回転させます。
		◇回転軸
			回転する方向を指定します。
			0=Z軸回転、1=Y軸回転、2=X軸回転

	○グループ制御もどき(子)
		グループ制御もどき(親)参照

	○色ずれ分解
		画像をRGBの要素に分解し、一つの要素だけを表示します。
		なお、R、G、Bの3つのオブジェクトを用意し、Rの画像にGとBの画像を加算合成すると、分解前の画像と同じになります。
		◇R,G,B
			R,G,Bの各色を選択します。
			0=赤、1=緑、2=青
		◇濃さ[%]
			オブジェクトの透明度を指定します。
			100%のときは、「色ずれ」フィルタで分解された場合と同じ透明度になります。

	○ランダム複数回転
		同じ画像を同じ位置に複数描画してバラバラに回転させます。
		図形や模様の画像に適用するとよいかもしれません。
		◇個数
			描画する個数を指定します。
		◇回転速度
			回転する速度を指定します。
		◇回転範囲
			回転の速さをランダムにします。
		◇拡大範囲
			オブジェクトの拡大率をランダムにします。
			数値は最小値を表します。例えば20と設定すると、20%～100%の間でランダムとなります。

	○円形並べ_レイヤーA
		レイヤー上に配置した異なる画像を円形に並べます。
		親となる画像に(A)を適用し、下の隣接するレイヤーに子の画像を配置し(B)を適用して下さい。
		親のパラメータを動かすと、全体が移動・回転等します。
		◇個数
			円形に並べる画像の数を指定します。
		◇距離
			円の半径を指定します。
		◇拡大率
			円全体の拡大率を指定します。
		◇回転
			円全体を回転させます。
		◇外向き
			チェックすると円形の方向を外向きにします。

	○円形並べ_レイヤーB
		円形並べ_レイヤーA参照

	○円形-カメラ_レイヤーA
		レイヤー上に配置した異なる画像を、立体的に円形に並べます。
		カメラ制御下で使用して下さい。
		親となる画像に(A)を適用し、下の隣接するレイヤーに子の画像を配置し(B)を適用して下さい。
		親のパラメータを動かすと、全体が移動・回転等します。
		◇個数
			円形に並べる画像の数を指定します。
		◇距離
			円の半径を指定します。
		◇拡大率
			円全体の拡大率を指定します。
		◇回転
			円全体を回転させます。

	○円形-カメラ_レイヤーB
		円形-カメラ_レイヤーA参照

	○カメラ深度ぼけPlus
		Z座標の数値によって「ぼかし」フィルタの強さが変わります。
		カメラ制御の深度ぼけのようなものです。
		◇範囲
			ぼかしの強さを指定します。
		◇Z基準
			ぼかす基準座標を設定します。
			この基準と同じ位置にある画像は、ぼかしの強さ=ゼロとなります。
			基準から離れるほど、強くぼかしフィルタがかかります。
		◇最大値
			ぼかしの範囲の最大値を設定します。
		◇奥をぼかす
			チェックするとZ基準より奥にあるものに、ぼかしフィルタがかかります。
		◇手前をぼかす
			チェックするとZ基準より手前にあるものに、ぼかしフィルタがかかります。

	○ハーフトーン風
		元画像の輝度に合わせて、図形やテキストを配置します。
		◇サイズ
			配置する図形のサイズを指定します。
			小さな値だと非常に重くなるので注意が必要です。
		◇size閾値
			「サイズ二値化」にチェックした場合に有効な項目です。
			図形を表示する割合を指定します。
		◇透明閾値
			「透明二値化」にチェックした場合に有効な項目です。
			図形を表示する割合を指定します。		
		◇回転
			個別の図形等を回転させます。
		◇図形(種類)
			配置する図形の種類を指定します。
		◇色(負の値で元色)
			配置する図形の色を指定します。
			負の値に設定すると、元画像の色を取得します。
		◇反転
			チェックすると図形を配置する位置を反転します。
		◇サイズ二値化
			チェックしていない場合は、元画像の輝度に応じて図形のサイズが異なりますが、
			チェックすると、図形のサイズが均一となります。
			図形を表示する割合は「size閾値」で指定します。
		◇透明二値化
			チェックしていない場合は、元画像の透明度に応じて図形の透明度が異なりますが、
			チェックすると、図形の透明度が均一となります。
			図形を表示する割合は「透明閾値」で指定します。
		◇個別拡大[%]
			配置した個別の図形の拡大率を変更します。
		◇文字
			チェックすると図形ではなく文字を配置します。
		◇文字(文字)
			文字の内容を入力して下さい。基本的に全角一文字のみ。
		◇文字(font)
			文字のフォントを指定します。

	○ハチの巣
		図形の六角形に適用して、ライン幅を小さくするとハチの巣状の模様になります。
		画像ループ2とほぼ同じです。
		◇X間隔,Y間隔
			各軸方向の間隔を指定します。
		◇X個数,Y個数
			各軸方向の個数を指定します。

	○移動トレース
		画像を移動させると、同じ画像がその後を追跡します。
		トラックバーでの移動に対応(スクリプトでの移動には対応していません)。
		◇個数
			描画する画像の数を指定します。
		◇時間差[s]
			次の画像が描画される時間間隔(秒)を指定します。
		◇終時間[s]
			最初の画像が(総時間-終時間)に達すると非表示になります。
			画像が最後の方で順番に消えていくように設定したい場合に使用します。

	○カクカクさん
		画像を移動・回転させると一定の間隔でカクカク移動・回転します。
		例えば、Z回転させると、時計の秒針のように回転します。
		◇間隔[s]
			カクカクさせる時間間隔(秒)を指定します。

	○透明度ランダム変化
		画像を「画像ループ2」のように配置し、それぞれの透明度をランダムに一定間隔で変化させます。
		◇X間隔,Y間隔
			各軸方向の間隔を指定します。
		◇X個数,Y個数
			各軸方向の画像の数を指定します。
		◇間隔[s]
			透明度を変化させる時間間隔を指定します。
		◇透明最小値[%]
			数値は透明度の最小値を表します。例えば20と設定すると、20%～100%の間でランダムとなります。

	○ランダム明滅
		画像を「画像ループ2」のように配置し、それぞれの透明度をランダムに明滅させます。
		◇X間隔,Y間隔
			各軸方向の間隔を指定します。
		◇X個数,Y個数
			各軸方向の画像の数を指定します。
		◇明滅最大[s]
			明滅の間隔の最大値を設定します。
			明滅最大～明滅最小の範囲で、ランダムな間隔で明滅します。
		◇明滅最小[s]
			明滅の間隔の最小値を設定します。
		◇開始位置[s]
			明滅の開始秒を指定します。
			ゼロにするとオブジェクトの最初で全ての画像が透明度1から明滅が開始されます。
			通常は変更する必要はない項目かと思います。
		◇閾値[%]
			画像が透明になる時間を増やし、表示されている画像の数を減らします。			

	○ランダム明滅2
		画像を「ランダム配置」のように配置し、それぞれの透明度をランダムに明滅させます。
		◇個数
			配置する画像の数を指定します。
		◇範囲[%]
			画像を配置する範囲を指定します。
			数値は100%=画面サイズが基準となります。
		◇移動速度
			画像がランダムに移動する速度を指定します。
		◇回転速度
			画像がランダムに回転する速度を指定します。
		◇明滅最大[秒]
			明滅の間隔の最大値を設定します。
			明滅最大～明滅最小の範囲で、ランダムな間隔で明滅します。
		◇明滅最小[秒]
			明滅の間隔の最小値を設定します。
		◇開始位置[秒]
			明滅の開始秒を指定します。
			ゼロにするとオブジェクトの最初で全ての画像が透明度1から明滅が開始されます。
			通常は変更する必要はない項目かと思います。
		◇閾値[%]
			画像が透明になる時間を増やし、表示されている画像の数を減らします。
		◇X,Y,Z軸範囲[%]
			X,Y,Z軸ごとに散らばる範囲の基準を指定します。
			数値は％で画面サイズが基準となっています(100%=画面サイズ)。
		◇X,Y,Z回転
			X,Y,Z軸ごとに、ランダムに回転させるか指定します。
		◇拡大最小[-%]
			画像の拡大率をランダムにします。
			数値は、最小の拡大率をマイナスで指定し、その範囲でランダムな拡大率になります。
			例えば、拡大最小=80(-%)と設定すると100-80=20%→20%～100%の間でランダムな拡大率となります。

	○ループANM
		簡単なアニメーションをループします。
		「アニメーション→待機」という動きを繰り返します。
		◇ループ[s]
			1回のループする時間を指定します。
		◇待機[s]
			ループ時間中の待機する秒数を指定します。
			「ループ[s]」の項目より小さな値を指定して下さい。
		◇拡大+%
			アニメーションする拡大率を指定します。
		◇縦横比
			アニメーションする縦横比を指定します。
		◇X,Y,Z
			アニメーションする移動距離をX,Y,Z軸ごとに指定します。
		◇X軸回転,Y軸回転,Z軸回転
			アニメーションする回転角度をX,Y,Z軸ごとに指定します。
		◇開始位置[s]
			アニメーションを開始する秒数を指定します。
			例えば、開始位置=2と設定すると、オブジェクトの2秒からアニメーションを開始します。
		◇待機2回
			チェックすると待機を2回します。
			「アニメーションの前半→待機→アニメーションの後半→待機」という動きを繰り返します。

	○TVオンオフ
		ブラウン管TVをつけたり、消した時のように、画像が伸縮して登場または退場します。
		◇時間
			登場する時間を指定します。マイナスで退場します。
		◇走査幅
			走査線の幅を指定します。
		◇縁調整[%]
			画像の周囲を丸く囲んで表示される影の大きさを調整します。
		◇影速度
			横線の上下に移動する黒い影の移動速度を指定します。
		◇縁あり
			チェックすると、周囲を縁取る影が表示されます。
		◇縁アス比
			チェックしない場合は、円状の影となります。
			チェックした場合は、適用した画像等の縦横比にあわせた楕円状の影となります。
		◇走査線あり
			チェックすると走査線が表示されます。
		◇走査線強さ
			走査線の透明度を指定します。
		◇走査線色
			走査線の色を指定します。
		◇影あり
			チェックすると、横線の上下に移動する黒い影が表示されます。
		◇影の幅
			影の幅を指定します。
		◇白線の幅
			登場の最初または退場の最後に表示される、白線の幅を指定します。

	○蝶
		蝶の画像に適用すると、蝶が羽ばたくようなアニメーションをします。
		あらかじめ画像の中心が蝶の中心になるように、画像を用意して下さい。
		カメラ制御下で使用して下さい。
		◇羽ばたき
			羽ばたきの速さを指定します。
		◇角度
			上下の羽ばたきの角度を指定します。
		◇上下
			画像全体を上下に動かします。その移動距離を指定。
		◇羽(手動)
			トラックバーで手動で羽ばたきを動かします。

	○カード表裏
		X軸又はY軸回転した際に、カードの表裏の関係のように別画像を表示します。
		拡張描画にし「裏面を表示しない」にチェックして使用して下さい。
		◇参照
			参照ボタンから裏面画像を読み込んでください。

	○ランダム配置表裏(A)
		表と裏が別の画像をランダムに配置します。
		使い方
			(A)→(B)の順番でアニメーション効果を適用して下さい。
			(B)の[参照]から裏面画像を読み込みます。
			拡張描画にして「裏面を表示しない」にチェックして下さい。
		◇個数
			配置する画像の数を指定します。
		◇範囲
			散らばる範囲を一括して調整します。
			ゼロにすると中心に収束し、トラックバーを直線移動することにより、
			だんだんと散らばっていくように表現することも出来ます。
		◇移動速度
			移動速度を設定すると、各画像がランダムな方向・速度で移動します。
		◇回転速度
			回転速度を設定すると、各画像がランダムな方向・速度で回転します。
		◇X,Y,Z軸範囲[%]
			X,Y,Z軸ごとに散らばる範囲の基準を指定します。
			数値は％で画面サイズが基準となっています(100%=画面サイズ)。
		◇X,Y,Z回転
			X,Y,Z軸ごとに、ランダムに回転させるか指定します。
		◇拡大最小[-%]
			画像の拡大率をランダムにします。
			数値は、最小の拡大率をマイナスで指定し、その範囲でランダムな拡大率になります。
			例えば、拡大最小=80(-%)と設定すると100-80=20%→20%～100%の間でランダムな拡大率となります。

	○ランダム配置表裏(B)
		◇裏反転
			裏面画像のみを左右反転します。
		◇参照
			裏面画像を読み込みます。

	○パーティクル表裏(A)
		表と裏が別の画像をパーティクル出力します。
		使い方
			(A)→(B)の順番でアニメーション効果を適用して下さい。
			(B)の[参照]から裏面画像を読み込みます。
			拡張描画にして「裏面を表示しない」にチェックして下さい。
		◇頻度
			パーティクルの出力頻度を指定します。
			数値は1秒間に出力されるパーティクルの数を表します。
		◇距離
			パーティクルが出力される距離を指定します。
			本家パーティクルとは異なり、この距離の間だけパーティクルが表示されます。
			長くしすぎると処理が重くなるので注意。
		◇速度
			パーティクルの出力される速度を指定します。
		◇方向
			パーティクルの出力される方向を指定します。下方向がゼロ度です。
		◇ランダム方向
			指定した角度の範囲でランダム方向に出力されます。360度で全方向です。
		◇透過速度[%]
			透明になる速度を指定します。
			透過速度=100で、ちょうどパーティクルの終点で透明度がゼロになるようにフェードします。
		◇拡大速度[%]
			オブジェクトが段々と拡大・縮小する速度です。
		◇ランダム速度[-%]
			パーティクルの出力される速度をランダムにします。
			数値は、例えば、ランダム速度=80で設定すると、100-80=20%→20%～100%の範囲でランダムとなります。
		◇ランダム拡大[-%]
			パーティクル一つひとつの拡大率をランダムにします。
			数値は、例えば、ランダム拡大=80で設定すると、100-80=20%→20%～100%の範囲でランダムとなります。
		◇ランダムZ角度
			出力時の角度がランダムになります。360度で完全にランダムな角度となります。
		◇X,Y,Z軸ランダム
			各座標から一定の範囲からランダムに出力されるようになります。
			例えば、X軸ランダム=800と設定すると、現在のX座標の位置から-400～400の範囲からランダムに出力されます。
		◇回転速度
			回転する速度を指定します。
			数値は、ひとつのパーティクルが発生してから消えるまで回転する角度を表します。
			例えば、回転速度=360とすると、発生してから消えるまでに1回転します。
		◇XY回転あり
			チェックするとX軸方向、Y軸方向にも回転し、3D的に回転するようになります。
		◇Z軸ランダム方向
			Z軸方向にも、ランダムな方向・距離で移動するようになります。
			数値は、ひとつのパーティクルが発生してから消えるまでに移動する距離の最大値です。

	○パーティクル表裏(B)
		◇裏反転
			裏面画像のみを左右反転します。
		◇参照
			裏面画像を読み込みます。

	○切れ込み
		図形の円に適用すると、切れ込みが入ったようにします。歯車のイメージ。
		◇深さ
			切れ込みの深さを指定します。
		◇角度調整
			切れ込みの斜め方向の角度を指定します。
		◇切込数
			切れ込みの数を指定します。2の倍数で設定させます。

	○もっと!?斜めクリッピング
		複数の斜めクリッピングを適用します。
		◇個数
			斜めクリッピングの数を指定します。
		◇幅
			斜めクリッピングの幅を指定します。
		◇角度
			斜めクリッピング全体の角度を変更します。
		◇中心X,中心Y
			斜めクリッピングの中心座標を指定します。

	○斜めクリッピングEX
		複数の斜めクリッピングをランダムに適用します。
		◇個数
			斜めクリッピングの数を指定します。
		◇幅
			斜めクリッピングの幅を指定します。
		◇増加範囲[+%]
			斜めクリッピングの幅をランダムに大きくします。
			例えば、増加範囲=150%と設定すると、100%～250%の範囲でランダムな幅となります。
		◇乱数
			ランダムに設定された結果を、別のランダム結果に変更します。
			数値は適当な整数を入力すればよいので、好みのランダム結果が表示されるようにいろいろ試すとよいかもしれません。
			また、直線移動することでランダム結果を次々に切り換えることも出来ます。

	○引き伸ばし
		画像の一部分を縦または横方向に引き伸ばします。
		◇伸度
			引き伸ばす距離を指定します。
		◇幅[%]
			引き伸ばす元画像の幅を指定します。
		◇基準[%]
			引き伸ばす元画像の位置を指定します。
		◇中心[%]
			引き伸ばした際に固定する部分を指定します。
			-100だと右(下)が固定され、+100だと左(上)が固定されます。
		◇縦方向
			チェックすると縦方向に引き伸ばされるようになります。
		◇幅部分を表示
			チェックすると引き伸ばす元画像部分を、透明度を下げて明示します。
			調整する際にはチェックすること推奨です。

	○ドット絵立体化
		ドット絵を3Dで立体的にします。
		カメラ制御下で使用して下さい。
		大きな画像に適用すると、非常に重くなるので、できるだけ小さなサイズのドット絵に適用して下さい。
		◇拡大率
			ドットの拡大率を指定します。
		◇Z幅
			Z軸方向の幅(厚み)を指定します。

	○画像ループMOV_A
		動画ファイルを「画像ループ2」のように複数配置し、それぞれ別の再生位置から再生します。
		図形などの適当なオブジェクトに(A)→(B)の順番で適用して下さい。
		(B)の参照ボタンから動画ファイルを読み込みます。
		◇X,Y間隔
			各画像間の間隔を、XY方向にそれぞれ指定します。
		◇X,Y個数
			各軸方向にループする回数を指定します。
		◇XY軸段違い
			XY軸平面上で段違いにループします。

	○画像ループMOV_B
		各動画はランダムな再生位置から再生が開始されます。
		動画の最後になると、最初から再生をはじめループします。
		◇再生速度[%]
			再生速度を指定します。
		◇再生位置[s]
			全体の再生位置をずらします。
		◇参照
			動画ファイルを読み込みます。


- ANM_ssd(アニメーション効果) -

	○震える_連動
		複数のオブジェクトに適用する場合に、それぞれの震える方向や角度を一致させます。
		各オブジェクトの先頭が同じフレームから始まっている場合に有効です。
		◇seed
			この値が同じオブジェクトは、同じように震えるようになります。

	○簡易変形マスク
		マスクが簡易変形します。本家「簡易変形」を改変したもの。
		◇時間経過[%]
			直線移動等にすると、終点のアンカーポイントが表示され、時間経過で変形できるようになります。
			0→100に設定すれば、それに応じて時間が経過します。
		◇サイズ
			マスクの輪郭が明確になります。図形を円にした場合などに分かりやすいです。
		◇ライン幅
			マスクのライン幅です。
		◇図形
			マスクの形を指定します。
		◇反転
			マスクを反転させます。
		◇座標
			アンカーポイントの座標を指定します。通常はドラッグして調整するので、触らなくてもいいかも。
		◇分割数
			マスクの輪郭のなめらかさを指定します。
		◇描画方法
			本家「簡易変形」と同様。

	○回転万華鏡
		単体の万華鏡っぽい効果です。テキストにかけるとよいかも。
		◇回転
			回転させて万華鏡っぽくします。
			「回転に連動」にチェックがある場合は、「連動角度」ごとに分割数が増えていきます。
		◇分割数
			「回転に連動」にチェックがない場合に有効。分割数を指定します。
		◇オフセット
			回転角度の最初の位置を指定します。
		◇回転に連動
			チェックがある場合は「連動角度」ごとに分割数が増えていきます。
		◇連動角度
			「回転に連動」にチェックがある場合に、この角度に達するごとに分割数が増えていきます。



- 加減速(アニメーション効果) -

	移動や各種フィルタ効果の変化を滑らかにします。

	●共通項目の説明
		◇時間[s]
			登場してくる時間を指定します。マイナスで退場します。
		◇加減速[1-5]
			加減速の度合いを指定します。
			減速度=1のときは減速移動なしで、数値が大きいほど減速度合いが強くなります。
		◇減/加/S[0-2]
			加減速の種類を指定します。
			0=減速して登場(加速して退場)
			1=加速して登場(減速して退場)
			2=S字カーブを描くように、段々と加速したあと、減速して登場(退場)します。
		◇項目名1,項目名2
			フィルタ効果の場合には、項目名に1や2の数値があります。
			項目名1は、通常のフィルタ効果の項目と同じものです。
			項目名2は、変化させる値を指定します。この値だけ加減速して登場してきます。
			なお、数値の付いていないものは、通常のフィルタ効果の項目と同じです。

	○ADC_標準登場
		X,Y,Z移動や拡大率、縦横比、X,Y,Z回転を滑らかに登場させます。
		従来の「加減速登場」を改訂したもの。
		◇時間[s]
			登場してくる時間を指定します。マイナスで退場します。
		◇方向
			登場してくる方向を指定します。
		◇距離
			登場してくる距離を指定します。
		◇拡大率
			登場してくる拡大率を指定します。
		◇加減速[1-5]
			加減速の度合いを指定します(詳細は「共通項目の説明」を参照)。
		◇減/加/S[0-2]
			加減速の種類を指定します(詳細は「共通項目の説明」を参照)。
		◇縦横比[%]
			登場してくる縦横比を指定します。
		◇Z軸
			Z軸方向の登場してくる距離を指定します。
		◇X回転,Y回転,Z回転
			登場してくる各軸の回転角度を指定します。

	○ADC_クリッピング
		「クリッピング」フィルタを加減速移動します。
		◇上1,下1,左1,右1
			「クリッピング」フィルタと同じ
		◇時間[s]
			登場してくる時間を指定します。マイナスで退場します。
		◇加減速[1-5]
			加減速の度合いを指定します(詳細は「共通項目の説明」を参照)。
		◇減/加/S[0-2]
			加減速の種類を指定します(詳細は「共通項目の説明」を参照)。
		◇上2,下2,左2,右2
			登場してくるクリッピングの量を指定します。

	○ADC_斜めクリッピング
		「斜めクリッピング」フィルタを加減速移動します。
		◇中心X1,中心Y1
			「斜めクリッピング」フィルタと同じ
		◇角度1
			「斜めクリッピング」フィルタと同じ
		◇幅1
			「斜めクリッピング」フィルタと同じ
		◇時間[s]
			登場してくる時間を指定します。マイナスで退場します。
		◇加減速[1-5]
			加減速の度合いを指定します(詳細は「共通項目の説明」を参照)。
		◇減/加/S[0-2]
			加減速の種類を指定します(詳細は「共通項目の説明」を参照)。
		◇中心X2,中心Y2
			登場してくる中心座標を指定します。
		◇角度2
			登場してくる角度を指定します。
		◇幅2
			登場してくる幅を指定します。
		◇ぼかし
			「斜めクリッピング」フィルタと同じ

	○ADC_マスク
		「マスク」フィルタのサイズ等を加減速移動します。
		重ねがけできません。
		◇回転1
			「マスク」フィルタと同じ
		◇サイズ1
			「マスク」フィルタと同じ
		◇縦横比1
			「マスク」フィルタと同じ
		◇ぼかし1
			「マスク」フィルタと同じ
		◇時間[s]
			登場してくる時間を指定します。マイナスで退場します。
		◇加減速[1-5]
			加減速の度合いを指定します(詳細は「共通項目の説明」を参照)。
		◇減/加/S[0-2]
			加減速の種類を指定します(詳細は「共通項目の説明」を参照)。
		◇マスクの種類
			マスクの種類を数値で指定します。
			1=円、2=四角形、3=三角形、4=五角形、5=六角形、6=星型
		◇X,Y
			「マスク」フィルタと同じ
		◇回転2
			登場してくる回転角度を指定します。
		◇サイズ2
			登場してくるサイズを指定します。
		◇縦横比2[%]
			登場してくる縦横比を指定します。
		◇ぼかし2
			登場してくるぼかしの範囲を指定します。
		◇反転
			「マスク」フィルタと同じ

	○ADC_ぼかし
		「ぼかし」フィルタの範囲を加減速移動します。
		◇時間[s]
			登場してくる時間を指定します。マイナスで退場します。
		◇範囲1
			「ぼかし」フィルタと同じ
		◇範囲2
			登場してくるぼかしの範囲を指定します。
		◇縦横比
		◇加減速[1-5]
			加減速の度合いを指定します(詳細は「共通項目の説明」を参照)。
		◇減/加/S[0-2]
			加減速の種類を指定します(詳細は「共通項目の説明」を参照)。
		◇光の強さ
			「ぼかし」フィルタと同じ
		◇サイズ固定
			「ぼかし」フィルタと同じ

	○ADC_放射ブラー
		「放射ブラー」フィルタの範囲を加減速移動します。
		◇時間[s]
			登場してくる時間を指定します。マイナスで退場します。
		◇範囲1
			「放射ブラー」フィルタと同じ
		◇範囲2
			登場してくるブラーの範囲を指定します。
		◇加減速[1-5]
			加減速の度合いを指定します(詳細は「共通項目の説明」を参照)。
		◇減/加/S[0-2]
			加減速の種類を指定します(詳細は「共通項目の説明」を参照)。
		◇X,Y
			「放射ブラー」フィルタと同じ
		◇サイズ固定
			「放射ブラー」フィルタと同じ

	○ADC_色ずれ
		「色ずれ」フィルタを加減速移動します。
		◇幅1
			「色ずれ」フィルタと同じ
		◇幅2
			登場してくる色ずれの幅を指定します。
		◇角度1
			「色ずれ」フィルタと同じ
		◇角度2
			登場してくる色ずれの角度を指定します。
		◇時間[s]
			登場してくる時間を指定します。マイナスで退場します。
		◇加減速[1-5]
			加減速の度合いを指定します(詳細は「共通項目の説明」を参照)。
		◇減/加/S[0-2]
			加減速の種類を指定します(詳細は「共通項目の説明」を参照)。
		◇色ずれの種類
			「色ずれ」フィルタと同じ

	○ADC_図形
		図形のサイズ等を加減速移動します。
		重ねがけできません。
		◇時間[s]
			登場してくる時間を指定します。マイナスで退場します。
		◇サイズ1
			図形のサイズを指定します。
		◇サイズ2
			登場してくる図形のサイズを指定します。
		◇ライン1
			図形のライン幅をしてします。
		◇加減速[1-5]
			加減速の度合いを指定します(詳細は「共通項目の説明」を参照)。
		◇減/加/S[0-2]
			加減速の種類を指定します(詳細は「共通項目の説明」を参照)。
		◇種類
			図形の種類を指定します。
		◇色
			図形の色を指定します。
		◇ライン幅2
			登場してくる図形のライン幅を指定します。

	○ADC_扇状
		扇型の図形を加減速移動します。
		重ねがけできません。
		◇時間[s]
			登場してくる時間を指定します。マイナスで退場します。
		◇中心角1
			扇型の中心角を指定します。
		◇サイズ1
			扇型のサイズを指定します。
		◇ライン1
			扇型のライン幅を指定します。
		◇加減速[1-5]
			加減速の度合いを指定します(詳細は「共通項目の説明」を参照)。
		◇減/加/S[0-2]
			加減速の種類を指定します(詳細は「共通項目の説明」を参照)。
		◇色
			扇型の色を指定します。
		◇中心2
			登場してくる中心角を指定します。
		◇サイズ2
			登場してくるサイズを指定します。
		◇ライン幅2
			登場してくるライン幅を指定します。


- 連番(アニメーション効果) -

	連番画像を、様々な配置で並べます。
	連番画像は、ファイル名が連続した数値で付けられているものを意味しています。
	連番ファイル名は「0001.bmp」のような形式にして下さい(他の拡張子でも可)。

	●使い方
		図形等の適当なオブジェクトに、必ず[A][B]の順番で追加して下さい。
		[A]の使い方は、"連番"と付かない名称の各スクリプトと同様です。
		[B]の項目で連番画像を読み込みます。

	●共通項目の説明
		◇連番桁
			連番部分の桁数を指定します。連番は0001から適用されます。
			例えば、連番桁=3であれば「001.bmp」という桁数のファイルを読み込みます。
		◇File数
			読み込むファイルの範囲(ファイル数)を指定します。
			例えば、File数=123であれば、0001～0123からランダムに出力されるようになります。
		◇順番
			「順番」項目があるものは、配置される順番を選択することが出来ます。
			ゼロの場合はランダムに出力され、1の場合は連番順に出力されます。

	○連番画像ループ[A]
		「画像ループ2」を連番画像で適用します。
		◇X,Y間隔
			各画像間の間隔を、XY方向にそれぞれ指定します。
		◇X,Y個数
			各軸方向にループする回数を指定します。
		◇Z間隔(pxl)
			Z軸方向にループした場合に、Z軸方向の間隔を指定します。
		◇Z個数
			Z軸方向にループする回数を指定します。
		◇XY軸段違い
			XY軸平面上で段違いにループします。
		◇ZY軸段違い
			ZY軸平面上で段違いにループします。

	○連番画像ループ[B]
		◇File数
			読み込むファイルの範囲を指定します(詳細は「共通項目の説明」を参照)。
		◇連番桁
			連番部分の桁数を指定します(詳細は「共通項目の説明」を参照)。	
		◇参照ファイル
			読み込む連番ファイルを指定します。

	○連番円形並べ[A]
		「円形並べ」を連番画像で適用します。
		◇個数
			画像の数を指定します。
		◇距離
			円の半径を指定します。
		◇拡大率
			画像の拡大率を指定します。
		◇回転
			画像の回転角度を指定します。
		◇回転なし
			チェックすると画像は、円の配置し応じて角度が変化しなくなり、同じ角度になります。

	○連番円形並べ[B]
		◇File数
			読み込むファイルの範囲を指定します(詳細は「共通項目の説明」を参照)。
		◇連番桁
			連番部分の桁数を指定します(詳細は「共通項目の説明」を参照)。	
		◇順番
			配置するファイルの順番を指定します(詳細は「共通項目の説明」を参照)。
		◇参照ファイル
			読み込む連番ファイルを指定します。

	○連番円形-カメラ[A]
		「円形並べ-カメラ」を連番画像で適用します。
		カメラ制御下で使用して下さい。
		◇個数
			画像の数を指定します。
		◇距離
			円の半径を指定します。
		◇拡大率
			全体の拡大率を指定します。
		◇回転
			全体を回転させます。
		◇Y軸回転なし
			配置に応じて、円形にY軸回転していますが、
			チェックをすると、Y軸回転せず、同じ方向を向くようになります。

	○連番円形-カメラ[B]
		◇File数
			読み込むファイルの範囲を指定します(詳細は「共通項目の説明」を参照)。
		◇連番桁
			連番部分の桁数を指定します(詳細は「共通項目の説明」を参照)。	
		◇順番
			配置するファイルの順番を指定します(詳細は「共通項目の説明」を参照)。
		◇参照ファイル
			読み込む連番ファイルを指定します。

	○連番ランダム配置[A]
		「ランダム配置」を連番画像で適用します。
		◇個数
			配置する画像の数を指定します。
		◇範囲
			散らばる範囲を一括して調整します。
			ゼロにすると中心に収束し、トラックバーを直線移動することにより、
			だんだんと散らばっていくように表現することも出来ます。
		◇移動速度
			移動速度を設定すると、各画像がランダムな方向・速度で移動します。
		◇回転速度
			回転速度を設定すると、各画像がランダムな方向・速度で回転します。
		◇X,Y,Z軸範囲[%]
			X,Y,Z軸ごとに散らばる範囲の基準を指定します。
			数値は％で画面サイズが基準となっています(100%=画面サイズ)。
		◇X,Y,Z回転
			X,Y,Z軸ごとに、ランダムに回転させるか指定します。
		◇拡大最小[-%]
			画像の拡大率をランダムにします。
			数値は、最小の拡大率をマイナスで指定し、その範囲でランダムな拡大率になります。
			例えば、拡大最小=80(-%)と設定すると100-80=20%→20%～100%の間でランダムな拡大率となります。

	○連番ランダム配置[B]
		◇File数
			読み込むファイルの範囲を指定します(詳細は「共通項目の説明」を参照)。
		◇連番桁
			連番部分の桁数を指定します(詳細は「共通項目の説明」を参照)。	
		◇参照ファイル
			読み込む連番ファイルを指定します。

	○連番パーティクル[A]
		「パーティクル再現」を連番画像で適用します。
		◇頻度
			パーティクルの出力頻度を指定します。
			数値は1秒間に出力されるパーティクルの数を表します。
		◇距離
			パーティクルが出力される距離を指定します。
			本家パーティクルとは異なり、この距離の間だけパーティクルが表示されます。
			長くしすぎると処理が重くなるので注意。
		◇速度
			パーティクルの出力される速度を指定します。
		◇方向
			パーティクルの出力される方向を指定します。下方向がゼロ度です。
		◇ランダム方向
			指定した角度の範囲でランダム方向に出力されます。360度で全方向です。
		◇透過速度[%]
			透明になる速度を指定します。
			透過速度=100で、ちょうどパーティクルの終点で透明度がゼロになるようにフェードします。
		◇拡大速度[%]
			オブジェクトが段々と拡大・縮小する速度です。
		◇ランダム速度[-%]
			パーティクルの出力される速度をランダムにします。
			数値は、例えば、ランダム速度=80で設定すると、100-80=20%→20%～100%の範囲でランダムとなります。
		◇ランダム拡大[-%]
			パーティクル一つひとつの拡大率をランダムにします。
			数値は、例えば、ランダム拡大=80で設定すると、100-80=20%→20%～100%の範囲でランダムとなります。
		◇ランダムZ角度
			出力時の角度がランダムになります。360度で完全にランダムな角度となります。
		◇X,Y,Z軸ランダム
			各座標から一定の範囲からランダムに出力されるようになります。
			例えば、X軸ランダム=800と設定すると、現在のX座標の位置から-400～400の範囲からランダムに出力されます。
		◇回転速度
			回転する速度を指定します。
			数値は、ひとつのパーティクルが発生してから消えるまで回転する角度を表します。
			例えば、回転速度=360とすると、発生してから消えるまでに1回転します。
		◇XY回転あり
			チェックするとX軸方向、Y軸方向にも回転し、3D的に回転するようになります。
		◇Z軸ランダム方向
			Z軸方向にも、ランダムな方向・距離で移動するようになります。
			数値は、ひとつのパーティクルが発生してから消えるまでに移動する距離の最大値です。

	○連番パーティクル[B]
		◇File数
			読み込むファイルの範囲を指定します(詳細は「共通項目の説明」を参照)。
		◇連番桁
			連番部分の桁数を指定します(詳細は「共通項目の説明」を参照)。	
		◇参照ファイル
			読み込む連番ファイルを指定します。

	○連番球状配置[A]
		こちらの球状配置スクリプトを連番画像にも対応させて改変したものです。
		http://www.nicovideo.jp/watch/sm13588483
		カメラ制御下で使用して下さい。
		◇半径
			球の半径を指定します。
		◇自転
			球を自転して回転させます。
		◇方向
			画像の方向を指定します。
			-1=指定なし、0=正面を向く、1=球の軸の外側を向く、2=球の軸の内側を向く、4=球の中心を向く
		◇極スケール[%]
			極(北極・南極)部分の画像のサイズを指定します。
			ゼロにすると、極に近づくほど画像のサイズが小さくなります。
		◇Xスケール,Yスケール,Zスケール[%]
			各軸方向のサイズを指定し、楕円形に伸縮させます。
		◇経線頂点
			経線方向の画像の数を指定します。
		◇緯線頂点
			緯線方向の画像の数を指定します。
		◇両極を描画
			チェックすると両極にある画像も描画します。

	○連番球状配置[B]
		◇File数
			読み込むファイルの範囲を指定します(詳細は「共通項目の説明」を参照)。
		◇連番桁
			連番部分の桁数を指定します(詳細は「共通項目の説明」を参照)。	
		◇参照ファイル
			読み込む連番ファイルを指定します。

	○連番らせん[A]
		こちらの螺旋配置スクリプトを連番画像にも対応させて改変したものです。
		http://www.nicovideo.jp/watch/sm13546528
		カメラ制御下で使用して下さい。
		◇個数
			描画する画像の数を指定します。
		◇回転差分
			次の画像を描画する際の角度の差分を指定します。
			らせんを巻く回数のイメージです。
		◇座標差分
			次の画像を描画する際の距離の差分を指定します。
			らせんの長さを伸ばすイメージです。
		◇回転
			らせん全体を回転させます。
		◇軸[0-2]
			らせんの軸を指定します。
			0=X軸、1=Y軸、2=Z軸
		◇向き[0-2]
			画像の向きを指定します。
			0=回転なし(手前向き固定)
			1=面を円の外側に向ける（軸がZの場合は上側を円の外側に向ける）
			2=面を円の内側に向ける（軸がZの場合は上側を円の内側に向ける）
		◇初期半径
			らせんの初期半径を指定します。
		◇半径差分
			次の画像を描画する際の半径の差分を指定します。
			らせんの広がりを調整するイメージです。
		◇大きさ
			次の画像を描画する際の拡大率の差分を指定します。
			画像がだんだん拡大したり縮小したりします。
		◇透明度
			次の画像を描画する際の透明度の差分を指定します。
			画像をだんだん透明にしたり、透明度を上げたりします。
		◇回転に同期移動
			チェックすると「回転」の項目で回転させた時、回転に同期して軸方向に移動するようになります。

	○連番らせん[B]
		◇File数
			読み込むファイルの範囲を指定します(詳細は「共通項目の説明」を参照)。
		◇連番桁
			連番部分の桁数を指定します(詳細は「共通項目の説明」を参照)。	
		◇順番
			配置するファイルの順番を指定します(詳細は「共通項目の説明」を参照)。
		◇参照ファイル
			読み込む連番ファイルを指定します。


- 音声(アニメーション効果) -

	音声に同期して各種アニメーションを作ります。
	編集中全体の音声を取得します。

	○拡大伸縮(音声同期)
		音声に同期して拡大率や縦横比が変化します。
		◇閾値
			指定した音域の音量が、閾値を超えた時に拡大伸縮します。
		◇音域
			音域を指定します。
			分割数より小さな値で設定して下さい。
		◇拡大率
			変化する拡大率を指定します。
		◇減衰率
			音量が閾値を超えると画像が拡大伸縮し、その後、だんだんと元の大きさに戻ります。
			その元の大きさに戻る速さを指定します。
		◇Xスケール[%]
			変化するX軸方向のサイズを指定します。
		◇Yスケール[%]
			変化するY軸方向のサイズを指定します。
		◇分割数
			音声データの分割数を指定します。
		◇補助線表示
			チェックすると、補助表示の音声波形が表示されます。
			調整後は非表示にして使用して下さい。
		◇補助線高さ[%]
			補助線の音声波形の高さを調整します。

	○方向ブラー(音声同期)
		音声に同期して「方向ブラー」フィルタがかかります。
		◇閾値
			指定した音域の音量が、閾値を超えた時に拡大伸縮します。
		◇音域
			音域を指定します。
			分割数より小さな値で設定して下さい。
		◇範囲
			「方向ブラー」フィルタの範囲を指定します。
		◇角度
			「方向ブラー」フィルタの角度を指定します。
		◇分割数
			音声データの分割数を指定します。
		◇色ずれ幅[%]
			「色ずれ」フィルタもかけることが出来ます。
			数値は、範囲×？％で適用されます。
		◇色ずれ種類[0-2]
			色ずれの種類を指定します。
			0=赤緑、1=赤青、2=緑青
		◇補助線表示
			チェックすると、補助表示の音声波形が表示されます。
			調整後は非表示にして使用して下さい。
		◇補助線高さ[%]
			補助線の音声波形の高さを調整します。

	○音声波形Plus
		画像等を音声波形の形で切り取ります。
		◇拡大率
			全体の拡大率を指定します。
			※この場合、設定ダイアログに元からある「拡大率」は正しく拡大が出来ません。
		◇高さ[%]
			波形の高さを調整します。
		◇分割数
			音声データの分割数を指定します。
			数値が大きいほど細かい波形となります。
		◇種類
			音声波形の種類を指定します。0～5の6種類。

	○音声波形Plus2
		画像等を音声波形の形で切り取ります。「音声波形Plus」とは異なるパターンです。
		◇拡大率
			全体の拡大率を指定します。
			※この場合、設定ダイアログに元からある「拡大率」は正しく拡大が出来ません。
		◇高さ[%]
			波形の高さを調整します。
		◇横分割
			横方向の分割数を指定します。
		◇縦分割
			縦方向の分割数を指定します。
		◇縦隙間
			縦方向の隙間の大きさを指定します。
		◇横隙間
			横方向の隙間の大きさを指定します。

	○音声波形Plus3
		別パターンの音声波形オブジェクトを作ります。
		適当な図形等のオブジェクトに適用して下さい。
		◇横幅
			横幅のサイズを指定します。
		◇縦幅%
			縦幅のサイズを調整します。
		◇ライン幅
			ライン幅をしていします。
		◇分割数
			音声データの分割数を指定します。
			数値が大きいほど細かい波形となります。
		◇色
			波形の色を指定します。

	○立体音声波形Plus
		音声波形で画像を切り取り、厚みを持った立体的なものにします。
		カメラ制御下で使用して下さい。
		◇拡大率
			全体の拡大率を指定します。
			※この場合、設定ダイアログに元からある「拡大率」は正しく拡大が出来ません。
		◇高さ[%]
			波形の高さを調整します。
		◇分割数
			音声データの分割数を指定します。
			数値が大きいほど細かい波形となります。
		◇奥行き
			Z軸方向の奥行きを指定します。

	○音声波形Plus2-ロール
		ロール状に変形した立体的な波形を作ります。
		カメラ制御下で使用して下さい。
		◇半径
			円の半径の指定します。
		◇高さ[%]
			波形の高さを調整します。
		◇横分割
			横方向の分割数を指定します。
		◇縦分割
			縦方向の分割数を指定します。
		◇縦隙間(%)
			縦方向の隙間の大きさを指定します。
		◇横隙間
			横方向の隙間の大きさを指定します。


- TA(アニメーション効果) -

	テキストオブジェクトに適用してください。
	基本的に「文字毎に個別オブジェクト」にチェックして使用することが前提です。

	●登場系のテキストアニメーションの共通する項目の説明
		◇時間[s]
			ひとつの文字が登場してくる時間を指定します。
			マイナスの時間にすると退場します。
		◇間隔[s]
			ひとつの文字が登場し始め、次の文字が登場し始める時間間隔を指定します。
		◇登場順[0-5]
			文字の登場順を指定します。
			0=前から順番に登場
			1=後ろから順番に登場
			2=ランダムな順番で登場
			3=ランダムな間隔で登場
			4=内側の文字から登場
			5=外側の文字から登場
		◇加減速[1-5]
			減速して滑らかに登場(加速して滑らかに退場)させるようにします。
			減速度=1のときは減速移動なしで、数値が大きいほど減速度合いが強くなります。

	また、登場系のスクリプトは基本的に重ねがけが可能なものが多いので、
	複数のスクリプトを重ねたり、登場用と退場用の2つのスクリプトを適用したりもできます。

	○TA標準登場
		XYZ移動、拡大、XYZ回転して登場させます。
		基本的な登場はこのスクリプトで可能となります。
		◇時間[s]
			登場時間を指定します。マイナスで退場します。
		◇間隔[s]
			文字の登場間隔を指定します。
		◇拡大率
			登場する拡大率を指定します。
		◇登場順[0-5]
			登場する順番を指定します(詳細は「共通項目の説明」を参照)。
		◇フェード
			チェックすると登場時にフェードします。
		◇X距離,Y距離,Z距離
			登場してくる距離をXYZ軸ごとに指定します。
		◇X軸回転,Y軸回転,Z軸回転
			登場してくる回転角を指定します。
		◇加減速[1-5]
			登場の加減速度の程度を指定します(詳細は「共通項目の説明」を参照)。
		◇タイプ
			チェックすると、全体的に登場してくるようになります。
			「時間」と「間隔」の値をある程度大きくするとよいかもしれません。

	○TA指定順で登場
		一文字毎に登場する順番を指定します。
		◇時間[s]
			登場時間を指定します。マイナスで退場します。
		◇間隔[s]
			文字の登場間隔を指定します。
		◇拡大率
			登場する拡大率を指定します。
		◇順番(0-9文字目),順番(10-19文字目),順番(20-29文字目)
			登場する順番を指定します。
			数値は、最初の2ケタが0文字目の順番、次の2ケタが1文字目の順番…という形になります。
			例えば、「あいうえお」というテキストの場合は次のようになります。
			0001020304＝00(あ)01(い)02(う)03(え)04(お)
			・あ→い→う→え→お、の順だと：0001020304
			・う→お→あ→え→い、の順だと：0204000301
			・お→え＝う＝い→あ、の順だと：0201010100
		◇フェード
			チェックすると登場時にフェードします。
		◇X距離,Y距離,Z距離
			登場してくる距離をXYZ軸ごとに指定します。
		◇X軸回転,Y軸回転,Z軸回転
			登場してくる回転角を指定します。
		◇加減速[1-5]
			登場の加減速度の程度を指定します(詳細は「共通項目の説明」を参照)。

	○TAぼかし登場
		テキストに「ぼかし」フィルタがかかりながら登場してきます。
		◇時間[s]
			登場時間を指定します。マイナスで退場します。
		◇間隔[s]
			文字の登場間隔を指定します。
		◇範囲
			登場してくるぼかしの強さ(範囲)を指定します。
		◇登場順[0-5]
			登場する順番を指定します(詳細は「共通項目の説明」を参照)。
		◇フェード
			チェックすると登場時にフェードします。
		◇縦横比(-100～100)
			ぼかしの方向(縦横比)を指定します。
		◇拡大率
			登場してくる拡大率を指定します。

	○TAぼかし登場2
		テキストが、縦長or横長になってぼやけて登場してきます。
		◇時間[s]
			登場時間を指定します。マイナスで退場します。
		◇間隔[s]
			文字の登場間隔を指定します。
		◇範囲
			ぼかしの強さ(範囲)を指定します。
		◇登場順[0-5]
			登場する順番を指定します(詳細は「共通項目の説明」を参照)。
		◇横方向
			ぼかしの方向が横方向になります。
		◇拡大率
			登場してくる拡大率を指定します。

	○TAぼかし登場3
		テキストが、横方向から小さくなって登場してきます。
		◇時間[s]
			登場時間を指定します。マイナスで退場します。
		◇間隔[s]
			文字の登場間隔を指定します。
		◇範囲
			ぼかしの強さ(範囲)を指定します。
		◇登場順[0-5]
			登場する順番を指定します(詳細は「共通項目の説明」を参照)。
		◇左から
			チェックすると左方向から登場してくるようになります。

	○TA画面外登場
		テキストが画面外から登場してきます。
		◇時間[s]
			登場時間を指定します。マイナスで退場します。
		◇間隔[s]
			文字の登場間隔を指定します。
		◇角度
			登場してくる方向を指定します。
		◇登場順[0-5]
			登場する順番を指定します(詳細は「共通項目の説明」を参照)。
		◇拡大率
			登場してくる拡大率を指定します。

	○TA拡大縮小で登場
		テキストが縮小→拡大して登場してきます。
		◇時間[s]
			登場時間を指定します。マイナスで退場します。
		◇間隔[s]
			文字の登場間隔を指定します。
		◇拡大率
			登場してくる拡大率を指定します。
		◇登場順[0-5]
			登場する順番を指定します(詳細は「共通項目の説明」を参照)。
		◇フェード
			チェックすると登場時にフェードします。
		◇X距離,Y距離,Z距離
			登場してくる距離をXYZ軸ごとに指定します。
		◇X軸回転,Y軸回転,Z軸回転
			登場してくる回転角を指定します。

	○TA拡大縮小で登場2
		テキストが指定した回数だけ拡大→縮小して登場してきます。
		ランダムな順で登場し、登場順の種類は変更できません。
		◇時間[s]
			登場時間を指定します。マイナスで退場します。
		◇間隔[s]
			文字の登場間隔を指定します。
		◇拡大率
			登場してくる拡大率を指定します。
			数値は最も大きく拡大した時の拡大率です。
		◇回数
			拡大縮小の回数を指定します。
		◇X距離,Y距離,Z距離
			登場してくる距離をXYZ軸ごとに指定します。
		◇加減速[1-5]
			登場の加減速度の程度を指定します(詳細は「共通項目の説明」を参照)。
		◇減衰[0-3]
			拡大縮小を重ねるごとに、拡大の幅が小さくなっていきますが、この減衰率を指定します。
			ゼロで減衰なしとなります。

	○TA分裂して登場
		テキストが上下または左右分裂して登場してきます。
		◇時間[s]
			登場時間を指定します。マイナスで退場します。
		◇間隔[s]
			文字の登場間隔を指定します。
		◇ぼかし
			ぼかしの強さ(範囲)を指定します。
		◇登場順[0-5]
			登場する順番を指定します(詳細は「共通項目の説明」を参照)。
		◇横方向
			チェックすると横方向からの登場になります。

	○TA字間移動で登場
		テキストが字間を狭めながら登場してきます。
		文字の揃え位置によって、登場の仕方が変わります。
		◇時間[s]
			登場時間を指定します。マイナスで退場します。
		◇字間率[%]
			登場してくる字間を指定します。
		◇中心座標
			登場の基準となる中心座標を調整します。
		◇加減速[1-5]
			登場の加減速度の程度を指定します(詳細は「共通項目の説明」を参照)。
		◇縦書き
			チェックすると縦書き(Y軸方向)の字間移動となります。
		◇フェード
			チェックすると登場時にフェードします。

	○TA陰陽玉登場
		陰陽玉のようなイメージで回って登場してきます。
		中央揃え[中]で使用して下さい。
		◇時間[s]
			登場時間を指定します。マイナスで退場します。
		◇角度
			回転する角度を指定します。
		◇加減速[1-5]
			登場の加減速度の程度を指定します(詳細は「共通項目の説明」を参照)。
		◇文字回転
			チェックすると、一文字毎に回転するようになります。

	○TAランダム文字で登場
		文字がランダムに切り替わりながら登場してきます。
		が、AviUtlスクリプトwikiに高機能版を公開されているので、そちらを使用すること推奨。
		◇時間[s]
			登場時間を指定します。マイナスで退場します。
		◇間隔[s]
			文字の登場間隔を指定します。
		◇文字間隔
			字間を調整します。
		◇拡大率
			登場してくる拡大率を指定します。
		◇切替フレーム
			文字が切り替わる速さをフレーム数で指定します。
		◇フェード
			チェックすると登場時にフェードします。
		◇Y軸
			登場してくるY軸方向の距離を指定します。

	○TA標準登場(行)
		テキストが1行ごとに登場してきます。
		※このスクリプトは「文字毎に個別オブジェクト」にチェックは不要です。
		なお、オブジェクトを横に切り取って個別に動かしているので、
		縁取り等のフィルタを太めにかけた場合は、上下の行と隣接する部分が横に切り取られる可能性があります。
		◇時間[s]
			登場時間を指定します。マイナスで退場します。
		◇間隔[s]
			一行ごとの登場間隔を指定します。
		◇行数
			行数を実際のテキストの行数に合わせて下さい。
		◇フェード
			チェックすると登場時にフェードします。
		◇拡大率[+%]
			登場してくる拡大率の増加分を指定します。
		◇X距離,Y距離,Z距離
			登場してくる距離をXYZ軸ごとに指定します。
		◇Z軸回転
			登場してくる回転角を指定します。
		◇加減速[1-5]
			登場の加減速度の程度を指定します(詳細は「共通項目の説明」を参照)。

	○TAランダム配置(行)
		テキストを一行ごとにランダムに配置します。
		※このスクリプトは「文字毎に個別オブジェクト」にチェックは不要です。
		◇行数
			行数を実際のテキストの行数に合わせて下さい。
		◇範囲
			散らばる範囲を一括して調整します。
			ゼロにすると中心に収束し、トラックバーを直線移動することにより、
			だんだんと散らばっていくように表現することも出来ます。
		◇移動速度
			移動速度を設定すると、各画像がランダムな方向・速度で移動します。
		◇回転速度
			回転速度を設定すると、各画像がランダムな方向・速度で回転します。
		◇X,Y,Z軸範囲[%]
			X,Y,Z軸ごとに散らばる範囲の基準を指定します。
			数値は％で画面サイズが基準となっています(100%=画面サイズ)。
		◇Z回転
			チェックするとランダムな角度で配置されます。
		◇拡大最小[-%]
			画像の拡大率をランダムにします。
			数値は、最小の拡大率をマイナスで指定し、その範囲でランダムな拡大率になります。
			例えば、拡大最小=80(-%)と設定すると100-80=20%→20%～100%の間でランダムな拡大率となります。

	○TA横スクロール(行)
		テキストを一行ごとにランダムに配置し、横方向にスクロールさせます。
		※このスクリプトは「文字毎に個別オブジェクト」にチェックは不要です。
		◇行数
			行数を実際のテキストの行数に合わせて下さい。
		◇移動速度
			スクロールする移動速度を指定します。
		◇X,Y,Z軸範囲[%]
			X,Y,Z軸ごとに散らばる範囲の基準を指定します。
			数値は％で画面サイズが基準となっています(100%=画面サイズ)。
		◇拡大最小[-%]
			一行毎のの拡大率をランダムにします。
			数値は、最小の拡大率をマイナスで指定し、その範囲でランダムな拡大率になります。
			例えば、拡大最小=80(-%)と設定すると100-80=20%→20%～100%の間でランダムな拡大率となります。
		◇速度最小[-%]
			移動速度をランダムにします。
			例えば、速度最小=80(-%)と設定すると100-80=20%→20%～100%の間でランダムな移動速度となります。

	○TA個別用の拡大回転
		「文字毎に個別オブジェクト」にチェックした場合でも、全体を拡大・回転させることが出来ます。
		ただし、複数の軸回転を組み合わせた場合は、正確な回転にならない場合があります。
		◇拡大率
			全体の拡大率を指定します。
		◇X軸回転,Y軸回転,Z軸回転
			各軸に沿って回転させます。

	○TA位置調整
		一文字毎のテキストの位置を、マウスドラッグで調整します。
		※テキストは16文字以下で使用して下さい。
		また、Ctrlを押しながら各アンカーポイント(破線の円)をドラッグすると、
		元座標を動かさずに全体を移動できます。
		◇散逸度[%]
			散逸度をゼロにすると、テキストの位置が元の位置に戻ります。
		◇座標
			数値入力により、各テキストの座標を指定することもできます。
			なお、マウスドラッグにより値が自動的に更新される項目なので、
			通常は調整する必要のない項目かもしれません。

	○TA位置(pos)関数
		テキストの配置を一文字毎に指定する関数を定義します。
		これ単体では何も表示されませんが、「スクリプト制御」を追加して関数を入力することで機能します。
		◇位置関数
			指定したインデックスの文字を、任意の座標、拡大率、縦横比、角度で配置します。
		◇形式
			pos(index,X座標,Y座標[,Z座標,拡大率,不透明度,縦横比,X軸回転,Y軸回転,Z軸回転])
		◇説明
			※[]内は省略可
			拡大率(1.0=等倍)、不透明度(0.0=透明/1.0=不透明)
			座標は初期位置からの座標を表します。


	○TA登場(pos2)関数
		テキストの登場方法を一文字毎に指定する関数を定義します。
		これ単体では何も表示されませんが、「スクリプト制御」を追加して関数を入力することで機能します。
		◇登場関数
			指定したインデックスの文字を、任意の座標、拡大率、縦横比、角度で登場させます。
		◇形式
			pos2(index,登場順[,X座標,Y座標,Z座標,拡大率,不透明度,縦横比,X軸回転,Y軸回転,Z軸回転])
		◇説明
			※[]内は省略可
			登場順(0～)、拡大率(1.0=等倍)、不透明度(0.0=透明/1.0=不透明)
			座標は初期位置からの座標を表します。
		◇時間[s]
			登場時間を指定します。マイナスで退場します。
		◇間隔[s]
			一行ごとの登場間隔を指定します。
		◇加減速[1-5]
			登場の加減速度の程度を指定します(詳細は「共通項目の説明」を参照)。

	○TAタイピングA
		テキストを表示する際にタイピングするように、アルファベットを一瞬表示させます。
		カーソルも表示されます。ただし厳密なものではなく、雰囲気だけのものです。
		テキストオブジェクトに(A)を適用し、すぐ下のレイヤーに別のテキストを配置＆(B)を適用して下さい。
		(B)のテキストには適当なアルファベットを入力すると、そのアルファベットが一瞬表示されます。
		◇表示速度
			テキストの表示速度を指定します。
			数値は1秒間に表示される文字の数を表します。

	○TAタイピングB
		テキストオブジェクトに適用して下さい。
		◇-横幅
			カーソルの横幅を指定した値だけ短くします、
		◇太さ
			カーソルの太さを指定します。
		◇色
			カーソルの色を指定します。

	○TA円形
		テキストを円形に並べます。
		◇距離
			円の半径を指定します。
		◇拡大率
			円全体の拡大率を指定します。
		◇回転
			円全体を回転させます。
		◇文字数
			ゼロのときは円状に均等に配置されますが、これを調整し円弧状に配置するようにできます。
		◇外向き
			チェックするとテキストの配置を外向きにします。

	○TA円形-カメラ
		テキストを円形に立体的に配置します。
		カメラ制御下で使用して下さい。
		◇距離
			円の半径を指定します。
		◇拡大率
			円全体の拡大率を指定します。
		◇回転
			円全体を回転させます。
		◇文字数
			ゼロのときは円状に均等に配置されますが、これを調整し円弧状に配置するようにできます。

	○TAランダム拡大
		テキストを一文字毎にランダムに拡大します。
		拡大(小)と拡大(大)の2つの拡大率がランダムにかかります。
		細かく拡大縮小しつつも、ときどき大きく拡大するイメージです。
		◇拡大(小)
			小さく拡大する場合の拡大率を指定します。
		◇拡大(大)
			大きく拡大する場合の拡大率を指定します。
		◇閾値(小)
			小さく拡大する頻度を指定します。
			まず、拡大(大)の判定がなされ、次に拡大(小)の判定がされます。
			なので基本的に「閾値(小)＜閾値(大)」という関係になるように設定して下さい。
		◇閾値(大)
			大きく拡大する頻度を指定します。
		◇切替時間[s]
			ランダムに切り替わる時間間隔(秒)を指定します。
			例えば、切替時間=0.5と設定すると、0.5秒ごとに拡大するかどうか判定されます。

	○TAバラバラ
		テキストが一定の範囲で、一文字毎にバラバラに配置されます。
		◇速さ
			テキストの移動や回転の速度を指定します。
		◇座標範囲
			テキストがバラバラになる座標の範囲を指定します。
		◇拡大範囲[+%]
			テキストのランダムな拡大率の範囲を指定します。
		◇回転範囲
			テキストの回転角度の範囲を指定します。
		◇初期位置
			チェックすると初期位置がバラバラの状態になります。
		◇Z軸方向
			チェックするとZ軸方向にも移動するようになります。
		◇乱数
			ランダムに設定された結果を、別のランダム結果に変更します。
			数値は適当な整数を入力すればよいので、好みのランダム結果が表示されるようにいろいろ試すとよいかもしれません。

	○TAバラバラ2
		テキストが一定の時間間隔で、一文字毎にバラバラに変化します。
		◇間隔[s]
			ランダムが切り替わる時間間隔(秒)を指定します。
		◇座標範囲
			テキストがバラバラになる座標の範囲を指定します。
		◇拡大範囲[+%]
			テキストのランダムな拡大率の範囲を指定します。
		◇回転範囲
			テキストの回転角度の範囲を指定します。
		◇Z軸方向
			チェックするとZ軸方向にも移動するようになります。
		◇乱数
			ランダムに設定された結果を、別のランダム結果に変更します。
			数値は適当な整数を入力すればよいので、好みのランダム結果が表示されるようにいろいろ試すとよいかもしれません。

	○TAランダム
		テキストがランダムに配置されます。
		◇散逸度
			テキストの散らばる度合いを指定します。
			直線移動などでゼロに移動させると、散らばっている状態から収束するように移動させることが出来ます。
		◇X速度,Y速度
			X,Y軸方向の移動速度を指定します。
		◇拡大/透明も変化
			チェックすると拡大率および透明度も変化するようになります。

	○TAランダム-カメラ
		テキストが3D的にランダムに配置されます。
		カメラ制御下で使用して下さい。
		◇散逸度
			テキストの散らばる度合いを指定します。
			直線移動などでゼロに移動させると、散らばっている状態から収束するように移動させることが出来ます。
		◇X速度,Y速度,Z速度
			X,Y,Z軸方向の移動速度を指定します。
		◇X軸範囲,Y軸範囲,Z軸範囲
			各軸方向の散らばる範囲を指定します。
		◇拡大/透明
			チェックすると拡大率と透明度もランダムに変化します。

	○TA標準移動
		一文字毎に文字が移動します。
		TA標準登場と似ていて、登場前の文字も表示されます。
		◇時間[s]
			登場時間を指定します。マイナスで退場します。
		◇間隔[s]
			文字の登場間隔を指定します。
		◇開始[s]
			最初の文字が移動し始めるまでの待機時間を指定します。
		◇登場順[0-5]
			登場する順番を指定します(詳細は「共通項目の説明」を参照)。
		◇拡大率
			登場する拡大率を指定します。
		◇X距離,Y距離,Z距離
			登場してくる距離をXYZ軸ごとに指定します。
		◇X軸回転,Y軸回転,Z軸回転
			登場してくる回転角を指定します。
		◇加減速[1-5]
			登場の加減速度の程度を指定します(詳細は「共通項目の説明」を参照)。
		◇減/加/S[0-2]
			加減速の種類を指定します(詳細は「加減速」の「共通項目の説明」を参照)。
		◇タイプ
			チェックすると、全体的に登場してくるようになります。
			「時間」と「間隔」の値をある程度大きくするとよいかもしれません。

	○TA位置調整で移動
		一文字毎に文字の位置をドラッグして指定し、その位置から文字が移動します。
		◇時間[s]
			登場時間を指定します。マイナスで退場します。
		◇間隔[s]
			文字の登場間隔を指定します。
		◇開始[s]
			最初の文字が移動し始めるまでの待機時間を指定します。
		◇登場順[0-5]
			登場する順番を指定します(詳細は「共通項目の説明」を参照)。
		◇加減速[1-5]
			登場の加減速度の程度を指定します(詳細は「共通項目の説明」を参照)。
		◇減/加/S[0-2]
			加減速の種類を指定します(詳細は「加減速」の「共通項目の説明」を参照)。
		◇タイプ
			チェックすると、全体的に登場してくるようになります。
			「時間」と「間隔」の値をある程度大きくするとよいかもしれません。
		◇座標
			ドラッグしたテキストの位置を、座標数値で指定することもできます。

	○TAテキスト配置(A)
		テキストを一文字ずつドラッグして配置します。
		テキストオブジェクトの制御文字でも同様のことができますが、それを柔軟?にしたイメージ
		図形などの適当なオブジェクトに、(A)→(B)の順に適用して下さい。
		◇基準サイズ
			テキストの基準となるサイズを指定します。
		◇Zランダム
			テキストを一文字毎に、ランダムにZ軸方向にずらします。
		◇回転ランダム
			テキストを一文字毎に、ランダムにZ軸回転します。
		◇移動速度
			テキストをランダム方向(X,Y,Z)に移動させます。
		◇英数字
			表示するテキストが英数字の場合はチェックします。
			なお、全角文字等と英数字が混ざったテキストには対応していません。
		◇テキスト
			表示したいテキストを入力します。最大16文字程度まで。
		◇(XY座標)
			XY座標を数値で指定することも出来ます。通常は設定する必要はありません。

	○TAテキスト配置(B)
		◇サイズ(基準:s)
			任意の文字のサイズを指定します。
			{1文字目のサイズ,2文字目のサイズ,…}というリストになってます。
			設定した基準サイズと同じ場合は、具体的な数値ではなく"s"でもOKです。
				【例】3文字目と5文字目のサイズを変える場合
					{s,s,100,s,120}
		◇基準フォント
			基準となるフォントを指定します。
		◇フォント(基準:f)
			任意の文字のフォントを指定します。
				【例】3文字目のフォントを指定する場合
					{f,f,"HGP明朝B"}
		◇基準色
			基準となるテキストの色を指定します。
		◇色(基準:c)
			任意の文字の色を指定します。
				【例】3文字目の色を指定する場合
					{c,c,0x00ffff}
		◇Z座標
			任意の文字のZ座標を指定します。
		◇Z回転
			任意の文字のZ回転を指定します。
		◇縦横比
			任意の文字の縦横比を指定します(-100～100)。

	○TA移動軌跡
		XY座標を直線移動などで移動させた場合に、その移動軌跡にそってテキストが移動します。
		◇位置基準
			テキストの位置を割合(%)で指定します。0：始点、100：終点
		◇字間
			テキストの字間を調整します。
		◇始終透明
			開始点または終了点にある文字が透明になります。
		◇時間基準
			オブジェクトの現在時間に応じて、テキストが始点から終点に移動します。
			チェックしている時は「位置基準」の項目が無効となります。


- FIGURE(カスタムオブジェクト) -

	○走査線2
		走査線を作ります。
		本家「走査線」と異なり、走査線の範囲を広げたり、フィルタ効果をかけたりすることが出来ます。
		◇ライン幅
			走査線の幅の大きさを指定します。
		◇間隔補正[%]
			走査線の空白部分の間隔を指定します。
			数値は、間隔=ライン幅×間隔補正(%)となります
		◇範囲拡大[%]
			走査線を描画する範囲を調整します。
			100%で画面サイズとなります。
		◇色
			走査線の色を指定します。

	○グリッド
		グリッドを作ります。
		◇ライン幅
			グリッドのライン幅を指定します。
		◇間隔
			グリッドの間隔(px)を指定します
		◇X範囲,Y範囲[%]
			X軸方向、Y軸方向のグリッドの範囲を指定します。
			画面サイズが100%となります。
		◇色
			グリッドの色を指定します。
		◇横線のみ
			チェックするとグリッドを横線のみの表示にします。

	○セピアノイズ
		セピアフィルム風のノイズを作ります。
		合成モードを「乗算」で合成して下さい。
		◇ゴミ閾値
			ゴミ状のノイズの量を調整します。
		◇ゴミサイズ
			ゴミ状のノイズのサイズを指定します。
		◇縦線閾値
			縦線状のノイズの量を調整します。
		◇輝度変化
			輝度の点滅の大きさを調整します。
			100で点滅がなくなります。
		◇縁色
			周囲を縁取っているグラデーションの色を指定します。
			デフォルトではセピア色に設定しています。
		◇ノイズ色
			ゴミ状のノイズや縦線ノイズの色を指定します。
		◇縁アス比
			周囲を縁取っているグラデーションの縦横比を選択します。
			チェックしない場合は円形となり、チェックすると画面のアス比と同じになります。
		◇縁あり
			チェックすると周囲をグラデーションで縁取ります。
		◇輝度反転
			チェックすると輝度を反転します。
			反転した場合は、合成モード「加算」などで合成して下さい。

	○ランダムライン
		縦・横・斜めのラインをランダムに描画します。
		ノイズ表現の一種として使うとよいかもしてません。
		◇本数
			ラインの本数を指定します。
		◇幅
			ラインの幅を指定します。
		◇幅[+%]
			ラインの幅をランダムに大きくします。
			例えば、幅[+%]=150%と設定すると、100%～250%の範囲でランダムな幅となります。
		◇斜閾値
			斜め方向のラインの割合を指定します。
			斜閾値=100で、ラインの3分の1が斜めになります。
		◇色
			ラインの色を指定します。
		◇ランダム変化
			チェックすると1フレーム毎にラインがランダムに変化します。

	○木漏れ日
		木漏れ日を作ります。
		オーバーレイなどで合成すると良いかもしれません。
		◇ぼかし
			木漏れ日の端部分をぼかします。

	○虹輪
		虹の輪を作ります。赤緑青の三色です。
		◇半径
			虹輪の半径を指定します。
		◇幅
			虹輪の幅を指定します。

	○虹輪2
		虹の輪を作ります。赤橙黄緑水青の六色です。
		◇半径
			虹輪の半径を指定します。
		◇幅
			虹輪の幅を指定します。
		◇ぼかし
			各色の間のぼかしの強さを指定します。

	○星型図形Plus
		星型の図形を作ります。星の内角を変更できることがポイント。
		◇半径
			星型のサイズ(半径)を指定します。
		◇内角
			星型の内角の大きさを指定します。
		◇ライン幅[%]
			ライン幅を指定します。
			数値は、100%だとライン幅ゼロ、0%だとライン表現なしになります。
		◇透明度
			ライン幅を設定した場合の、空白部分の透明度を指定します。
		◇色
			図形の色を指定します。

	○きらり
		「きらり」とした感じの図形オブジェクトを作ります。
		◇サイズ
			図形のサイズを指定します。
		◇幅[%]
			十字部分の幅のサイズを調整します。
		◇円透明
			ぼやけた円のグラデーションの透明度を指定します。
		◇色
			図形の色を指定します。

	○矢印
		矢印の図形を作ります。矢印を伸ばしたい時に便利です。
		◇長さ
			矢印の長さを指定します。
		◇幅
			矢印の幅を指定します。
		◇3サイズ
			矢印の頭の三角形部分のサイズを指定します。
		◇3横幅
			矢印の頭の三角形部分の横幅を指定します。
		◇色
			矢印の色を指定します。

	○多角形ライン
		多角形のラインを作ります。
		「辺数」と「回転数」の組み合わせでいくつかの図形を作ることが出来ます。
		◇半径
			図形のサイズ(半径)を指定します。
		◇ライン幅
			ライン幅を調整します。
		◇辺数
			描画する辺の数を指定します。
		◇回転数
			辺の描画を何回転して描画するか設定します。
			2以上の値にすると、正多角形以外のラインを描画することも出来ます。
		◇色
			ラインの色を指定します。

	○単色フェード
		オブジェクトの最初と最後で、図形(背景)がフェードアウト・インします。
		頻繁に単色でフェードイン・アウトを繰り返す場合に、レイヤーの一番下に置いて使うと便利かもしれません。
		◇イン[s]
			冒頭部分のフェード時間を指定します。
		◇アウト[s]
			終了部分のフェード時間を指定します。
		◇色
			図形の色を指定します。

	○色変換(RGB-16進数)
		取得した色を16進数のコードで表示します。色選択の補助に。
		◇サイズ
			テキストのサイズを指定します。
		◇タイプ[0-4]
			テキストの装飾タイプを指定します。
			0=標準文字、1=影付き文字、2=影付き文字(薄)、3=縁取り文字、4=縁取り文字(細)
		◇色
			ここで16進数のコード知りたい色を取得して下さい。。

	○市松模様N
		市松模様を作ります。
		◇サイズ
			一つの四角形のサイズを指定します。
		◇横回数,縦回数
			横または縦方向に繰り返す回数を指定します。		
		◇色1,色2
			市松模様の色を指定します。

	○チェック柄
		チェック柄を作ります。
		◇横回数,縦回数
			横または縦方向に繰り返す回数を指定します。	
		◇背景色
			背景部分の色を指定します。
		◇チェック色
			チェック部分の色を指定します。

	○波状の粒
		図形を波形状にランダムに配置します。
		◇個数
			配置する図形の数を指定します。
		◇サイズ
			図形のサイズを指定します。
		◇高さ
			波全体の高さを指定します。
		◇幅
			波全体の幅を指定します。
		◇種類
			配置する図形の種類を指定します。
		◇色
			配置する図形の色を指定します。
		◇ライン幅
			配置する図形のライン幅を指定します。
		◇変化速度[ms]
			ランダムに切り替わる時間間隔を指定します。
		◇増加率
			端になるほど波の高さが大きくなる増加率を指定します。

	○カラフルパーティクル
		ランダムな色の図形をパーティクル出力します。
		使い方は「パーティクル再現」とほぼ同様です。
		◇頻度
			パーティクルの出力頻度を指定します。
			数値は1秒間に出力されるパーティクルの数を表します。
		◇距離
			パーティクルが出力される距離を指定します。
			本家パーティクルとは異なり、この距離の間だけパーティクルが表示されます。
			長くしすぎると処理が重くなるので注意。
		◇速度
			パーティクルの出力される速度を指定します。
		◇方向
			パーティクルの出力される方向を指定します。下方向がゼロ度です。
		◇種類
			図形の種類を指定します。
		◇サイズ
			図形のサイズを指定します。
		◇ライン幅
			図形のライン幅を指定します。
		◇ランダム方向
			指定した角度の範囲でランダム方向に出力されます。360度で全方向です。
		◇ランダム速度[-%]
			パーティクルの出力される速度をランダムにします。
			数値は、例えば、ランダム速度=80で設定すると、100-80=20%→20%～100%の範囲でランダムとなります。
		◇透過速度[%]
			透明になる速度を指定します。
			透過速度=100で、ちょうどパーティクルの終点で透明度がゼロになるようにフェードします。
		◇拡大速度[%]
			オブジェクトが段々と拡大・縮小する速度です。
		◇ランダムZ角度
			出力時の角度がランダムになります。360度で完全にランダムな角度となります。
		◇X,Y軸ランダム
			各座標から一定の範囲からランダムに出力されるようになります。
			例えば、X軸ランダム=800と設定すると、現在のX座標の位置から-400～400の範囲からランダムに出力されます。
		◇ランダムZ回転
			回転する速度を指定します。
			数値は、ひとつのパーティクルが発生してから消えるまで回転する角度を表します。
			例えば、回転速度=360とすると、発生してから消えるまでに1回転します。
		◇ランダム拡大[-%]
			パーティクル一つひとつの拡大率をランダムにします。
			数値は、例えば、ランダム拡大=80で設定すると、100-80=20%→20%～100%の範囲でランダムとなります。

	○カラフルランダム配置
		ランダムな色の図形をランダムに配置します。
		◇個数
			配置する図形の数を指定します。
		◇X幅[%],Y幅[%],Z幅[%]
			図形を配置する範囲をしていします。
			100%が画面サイズとなります。
		◇種類
			図形の種類を指定します。
		◇サイズ
			図形のサイズを指定します。
		◇ライン幅
			図形のライン幅を指定します。
		◇ランダムZ回転
			ランダムに回転する速さを指定します。
		◇ランダム拡大[-%]
			図形一つひとつの拡大率をランダムにします。
			数値は、例えば、ランダム拡大=80で設定すると、100-80=20%→20%～100%の範囲でランダムとなります。

	○ランダムに輪が出現
		ランダムに一定間隔で図形が出現し、フェードアウトしていきます。
		◇時間[s]
			ひとつのオブジェクトが登場＆フェードしていく時間を指定します。
		◇間隔[s]
			次のオブジェクトが出現する時間間隔を指定します。
		◇サイズ
			図形のサイズを指定します。
		◇ライン幅
			図形のライン幅を指定します。
		◇種類
			図形の種類を指定します。
		◇色
			図形の色を指定します。
		◇カラフル
			チェックするとランダムな色になります。
		◇ランダム角度
			チェックするとランダムな角度で出力されるようになります。
		◇仮想bufサイズ[%]
			図形が表示される範囲を指定します。
			100%が画面サイズとなります。

	○メーター
		時間と共に長さが短くなるメーターを作ります。
		オブジェクトの長さが基準で、オブジェクトの最後で長さゼロになります。
		◇高さ
			メーターの高さを指定します。
		◇長さ
			メーターの最初の長さを指定します。
		◇モード[-1～1]
			メーターの固定位置を指定します。
			-1=左固定、0=中央固定、1=右固定
		◇色1,色2,色3,色4,色5
			メータの色を指定します。5段階に分かれています。

	○カメラ補助線
		カメラ制御の補助線を表示します。
		カメラ制御下で使用して下さい。
		◇サイズ
			全体のサイズを指定します。
		◇ライン幅
			各ラインの幅を指定します。
		◇テキスト
			テキストのフォントサイズを指定します。
		◇色
			XZ平面の床の色を指定します。

	○立体図形(正多角形)-カメラ
		正多角形の立体図形を作ります。
		カメラ制御下で使用して下さい。
		◇サイズ
			図形のサイズを指定します。
		◇奥行き
			図形の奥行き(厚み)を指定します。
		◇ライン幅
			図形のライン幅を指定します。
		◇角数[3-6]
			多角形の角数を指定します。
		◇色
			図形の色を指定します。

	○立体図形(円)-カメラ
		円柱の立体図形を作ります。
		カメラ制御下で使用して下さい。
		◇サイズ
			図形のサイズを指定します。
		◇奥行き
			図形の奥行き(厚み)を指定します。
		◇ライン幅
			図形のライン幅を指定します。
		◇分割数
			側面部分の分割数を指定します。
		◇色
			図形の色を指定します。

	○立体図形(星)-カメラ
		星型の立体図形を作ります。
		カメラ制御下で使用して下さい。
		◇サイズ
			図形のサイズを指定します。
		◇奥行き
			図形の奥行き(厚み)を指定します。
		◇ライン幅
			図形のライン幅を指定します。
		◇色
			図形の色を指定します。

	○立体図形(長方形)-カメラ
		長方形の立体図形を作ります。
		カメラ制御下で使用して下さい。
		星型の立体図形を作ります。
		カメラ制御下で使用して下さい。
		◇サイズ
			図形のサイズを指定します。
		◇奥行き
			図形の奥行き(厚み)を指定します。
		◇ライン幅
			図形のライン幅を指定します。
		◇縦横比
			図形の縦横比を指定します。
		◇色
			図形の色を指定します。

	○縁つき立方体-カメラ
		縁がついた立方体の図形を作ります。
		カメラ制御下で使用して下さい。
		◇サイズ
			図形のサイズを指定します。
		◇ライン幅
			縁部分の幅を指定します。
		◇透明度
			内側部分の透明度を指定します。
		◇明滅間隔[s]
			「明滅あり」にチェックした場合に有効です。
			明滅させる時間間隔[s]を指定します。
		◇縁色,内色
			縁部分または内側部分の色をしていします。
		◇明滅あり
			チェックすると、内側部分が明滅します。

	○シンプル魔法陣
		シンプルな魔法陣を作ります。
		◇半径
			魔法陣の半径を指定します。
			魔法陣内側の多角形部分のサイズとなります。
		◇角幅
			魔法陣の多角形部分の幅を指定します。
		◇円幅
			円の幅を指定します。
		◇種類[1-7]
			多角形部分をいくつかの種類から選択します。
		◇色
			魔法陣の色を指定します。
		◇回転速度
			魔法陣の回転速度を指定します。
			回転速度=1だと10秒間で1回転します。マイナスにすると反対方向に回転します。
			ゼロにすると回転しません。
		◇図形2つ
			多角形部分の図形を2つ描画します。
			各図形は反対方向に回転します。
		◇外円調整(%)
			一番外側の円の半径を調整します。
		◇text(英数字のみ)
			円に描画するテキスト文字を入力して下さい。
			英数字のみ限定です。
		◇textサイズ
			テキストのフォントサイズを指定します。
		◇textフォント
			テキストのフォントを指定します。
		◇text半径調整(%)
			テキストを配置する半径を調整します。
		◇textサイズ調整(%)
			テキストのフォントサイズのみを調整します。

	○AviUtlアイコン
		AviUtlのアイコン画像を描画します。
		◇サイズ
			画像のサイズを指定します。
		◇色1,色2
			画像の色を指定します。

	○マウスポインタ
		マウスポインタ(矢印)の画像を描画します。誰得。
		◇サイズ
			画像のサイズを指定します。
		◇色,縁色
			画像の色や縁部分の色を指定します。

	○小屋
		Aviutlで家が建ちます。
		(カメラ制御下で使用して下さい。)
		◇サイズ
			家の大きさを指定します。
		◇横幅比
			家の横幅の広さを指定します。
		◇奥行き
			家の奥行きを指定します。
		◇角度
			屋根部分の角度を指定します。
		◇壁色,屋根色
			壁部分および屋根部分の色を指定します。
		◇ひさし1
			屋根の正面部分のひさしの大きさを指定します。
		◇ひさし2
			屋根の側面部分のひさしの大きさを指定します。
		◇屋根隙間
			壁と屋根の間に隙間を作ります。
			隙間=ゼロだと、壁が屋根を貫通してしまう場合があるので注意。


- LOAD(カスタムオブジェクト) -

	○ランダム配置MOV
		動画ファイルを、それぞれ再生位置を変えて、ランダムな位置に配置されます。
		それぞれの動画は、再生が終了すると最初から再生が繰り返され、ループするようになっています。
		◇個数
			ランダムに配置する数を指定します。
		◇再生速度
			動画の再生速度を指定します。
		◇再生位置
			全体の動画の再生位置をずらして調整します。
		◇配置範囲[%]
			動画を配置する範囲を指定します。
			100%が画面サイズとなります。
		◇参照ファイル
			動画ファイルを読み込みます。

	○動画再生オプション
		動画ファイルを加速して再生させたりします。
		再生が終了すると最初から再生が繰り返され、ループするようになっています。
		◇開始
			再生の開始位置を指定します。
		◇速度[%]
			再生速度を指定します。
		◇加速度
			動画の再生を段々と加速させます。
			マイナスでは減速し、再生速度ゼロで停止します。
		◇コマ落[s]
			コマ落ちのように、指定した時間間隔[s]でカクカクした再生となります。
		◇参照ファイル
			動画ファイルを読み込みます。

	○動画再生オプション(連番入力)
		連番画像を加速して再生させます。
		連番画像の取り扱いは、連番スクリプトの項目を参照。
		◇File数
			読み込むファイルの範囲を指定します。
		◇連番桁
			連番部分の桁数を指定します。	
		◇速度[%]
			再生速度を指定します。
		◇加速度
			動画の再生を段々と加速させます。
			マイナスでは減速し、再生速度ゼロで停止します。
		◇参照ファイル
			連番画像を読み込みます。


- 履歴 -

	[2013/7/7]
	「@加減速TRA.tra」「カクカク.tra」を追加。

	[2013/7/28]
	「ランダム軌道.anm」を追加。

	[2013/7/29]
	「ランダム軌道.anm」を修正。「ランダム軌道_方向.anm」を追加。

	[2013/8/1]
	「多角形_震える.obj」を追加。

	[2013/10/4]
	「ふらふら.tra」「裏面塗りつぶし.anm」「連番画像読込_改変.obj」を追加。

	[2013/10/11]
	「円矢印.obj」を追加。

	[2014/1/19]
	「TA字間XY.anm」を追加。

	[2014/2/9]
	「TA数珠つなぎ」を追加。

	[2014/2/15]
	「TA操作SP.anm」を追加。

	[2014/4/12]
	「回転万華鏡.anm」を追加。

	[2014/5/3]
	「可変速度動画読込.obj」を追加。「斜めブラインド」を修正。

	[2014/5/5]
	「三角括弧.obj」「透過判定図形並べ.anm」を追加。

	[2014/8/31]
	「TA範囲指定で変化.anm」を追加。

	[2014/9/4]
	「@範囲指定で変化.anm」を追加。「TA範囲指定で変化.anm」を修正。

	[2014/12/21]
	「ホログラム.obj」を追加。

	[2014/12/23]
	「タータンチェック_ISTN.obj」を追加。

	[2014/12/31]
	「集中線plus.obj」を追加。

	[2015/1/1]
	「日の出.obj」を追加。

	[2015/1/3]
	「千鳥格子.obj」「麻の葉模様.obj」「矢がすり.obj」を追加。

	[2015/1/7]
	「TA回転停止で登場.anm」を追加。

	[2015/1/10]
	「しきい値ランダム移動.tra」を追加。

	[2015/3/13]
	「分度器.obj」を追加。

	[2015/7/3]
	「TA標準登場2.anm」を追加。

	[2015/9/12]
	「ランダムラインEX.obj」を追加。

	[2015/9/14]
	「2値化ノイズ.anm」を追加。

	[2016/8/28]
	「紙飛行機.obj」を追加。